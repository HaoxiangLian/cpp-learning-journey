# 第 17 天：声明、定义、名称查找、链接与 ODR｜求职版练习

## 作答规则

- 先预测，再编译或运行；实际结果与预测不同时，必须记录差异。
- 所有代码使用 C++17，并开启 `-Wall -Wextra -pedantic`。
- 故意失败的程序必须写明失败阶段、关键诊断和修复依据。
- 每道题的答案直接写在题目下方。
- 四类必做任务全部通过后，本单元才算完成。

---

## A｜基础概念检查

1. 名称查找成功为什么仍可能链接失败？

   > 

2. 命名空间作用域 `static` 与函数局部 `static` 分别影响什么？

   > 

3. 结合本课最小代码，按顺序说明其中至少三个对象、值或文件发生了什么变化。

   > 

---

## B｜可运行编程任务

### 任务

建立 `config.hpp`、`config.cpp` 和两个使用方，使用 `extern` 共享运行期配置，使用 `inline constexpr` 共享编译期常量。

### 起始代码

```cpp
// config.hpp
#pragma once
extern int control_rate_hz;
inline constexpr double max_force{10.0};

// config.cpp
#include "config.hpp"
int control_rate_hz{125};
```

### 预期结果

```text
两个使用方观察到同一个 `control_rate_hz` 对象
```

### 验收标准

- [ ] 普通变量只有一个定义
- [ ] 头文件可被多个翻译单元包含
- [ ] 能用 `nm -C` 观察符号
- [ ] 能分别解释名称查找、链接和 ODR

### 你的实现

```cpp

```

使用的构建命令：

```bash

```

实际输出：

```text

```

预测与实际的差异：

> 

---

## C｜调试任务

### 故障代码

```cpp
// bad.hpp
int control_rate_hz{125};

// a.cpp 和 b.cpp 都 include bad.hpp
```

### 调试要求

构建两个使用方，记录重复定义。至少给出 `extern` 分离定义或 C++17 `inline` 变量两种修复思路，并解释适用差异。

运行或构建前的预测：

> 

错误发生的阶段：

> 

关键诊断或异常现象：

```text

```

根本原因：

> 

修复后的代码：

```cpp

```

为什么这个修复有效：

> 

---

## D｜求职面试口述

先用 30～60 秒回答：

> 名称查找成功为什么仍可能链接失败？

答案必须能够回到本课实验中的具体事实，例如：

- 名称查找决定当前代码中的 `control_rate_hz` 指向哪个可见声明
- `extern` 声明不在头文件中创建新的变量定义
- `config.cpp` 提供唯一普通定义
- 外部链接允许不同翻译单元的声明指向同一实体

第一次口述提纲：

> 

面试追问：

> 命名空间作用域 `static` 与函数局部 `static` 分别影响什么？

你的回答：

> 

删掉任何只能背诵、不能由代码或诊断推导出的句子，再写第二版提纲：

> 

---

## 学习复盘

今天最清楚的机制：

> 

仍然不能通过代码解释的地方：

> 

本单元是否达到四类任务的验收标准：

> 
