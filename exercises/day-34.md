# 第 34 天｜求职版练习

> 配套讲义：[测试、静态分析与代码审查](../days/第34天-测试、静态分析与代码审查.md)

## 作答规则

- 先预测，再编译或运行；实际与预测不同就记录第一处差异。
- 所有 C++ 代码使用 C++17，并启用 `-Wall -Wextra -pedantic`；线程题额外使用 `-pthread`。
- 故意失败的程序要写清失败阶段、关键诊断、根因和验证证据。
- 每道题的答案直接写在题目下方。
- 四类任务全部达到验收标准，本单元才算完成。

---

## A｜基础概念检查

1. 为什么只有 happy path 的测试价值有限？

   > 

2. 浮点容差应该依据什么选择？

   > 

3. 静态分析与运行测试各自看不到什么？

   > 

4. 用本课最小示例说明：哪个对象、文件或调用在什么语句前后发生了变化？

   > 

---

## B｜可运行编程任务

### 任务

为 `clamp_position` 添加参数非法、等于上下边界、极小浮点偏差的测试；让测试失败时进程返回非零状态。

### 起始代码

```cpp
#include <cmath>
#include <iostream>
#include <stdexcept>

// 复制讲义中的 clamp_position 和 expect_near

int main() {
    // TODO: inside / below / above / low boundary / high boundary
    // TODO: low > high 必须抛 invalid_argument
}
```

### 预期结果

```text
[PASS] 至少 6 项检查；故意把一个期望值改错时，测试进程必须失败。
```

### 验收标准

- [ ] 测试从契约覆盖正常、边界和失败路径
- [ ] 浮点容差有明确依据
- [ ] 保留一次先失败再修复的记录

### 你的实现

```cpp

```

使用的构建命令：

```bash

```

运行前的输出预测：

> 

实际输出：

```text

```

预测与实际的第一处差异：

> 

---

## C｜调试任务

### 故障代码

```cpp
double clamp_position(double value, double low, double high) {
    if (value < low) {
        return high;  // 写反了
    }
    if (value > high) {
        return high;
    }
    return value;
}

// 只测 clamp_position(0.2, -1, 1)，仍会通过。
```

### 调试要求

先不要修改。判断问题首先出现在编译、链接、运行、结果还是接口设计；运行或构建后，只保留最关键的诊断。修复时说明为什么最小修改能针对根因，而不是只让报错消失。

运行或构建前的预测：

> 

问题首先出现的阶段：

> 

关键诊断或异常现象：

```text

```

根本原因：

> 

修复后的代码：

```cpp

```

为什么这个修复有效：

> 

修复后的验证输入与结果：

```text

```

---

## D｜求职面试口述

先用 30～60 秒回答本课核心问题。回答必须包含：

- 至少覆盖正常、上下越界和边界
- 失败时进程返回非成功状态
- 能给出可复现测试命令

参考结构是“具体问题 → 对象或文件怎样变化 → 使用的规则 → 一个限制”，不要堆术语。

第一次口述提纲：

> 

面试官追问：

> 为什么只有 happy path 的测试价值有限？

你的回答：

> 

再追问：

> 浮点容差应该依据什么选择？

你的回答：

> 

删掉所有不能回到代码、输出、诊断或测试证据的句子，再写第二版提纲：

> 

---

## 学习复盘

今天最清楚的机制：

> 

仍然不能通过代码解释的地方：

> 

我能否在不看讲义时复现程序和故障：

> 

本单元是否达到四类任务的验收标准：

> 
