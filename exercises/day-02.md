# 第 2 天：变量、类型、初始化与表达式｜求职版练习

## 作答规则

- 先预测，再编译或运行；实际结果与预测不同时，必须记录差异。
- 所有代码使用 C++17，并开启 `-Wall -Wextra -pedantic`。
- 故意失败的程序必须写明失败阶段、关键诊断和修复依据。
- 每道题的答案直接写在题目下方。
- 四类必做任务全部通过后，本单元才算完成。

---

## A｜基础概念检查

1. `double x = 3.0;` 中的等号为什么仍是初始化，不是赋值？

   > 

2. 类型除了决定占用空间，还影响什么？

   > 

3. 结合本课最小代码，按顺序说明其中至少三个对象、值或文件发生了什么变化。

   > 

---

## B｜可运行编程任务

### 任务

读取目标角度和当前角度，计算并输出误差。变量必须在定义时初始化。

### 起始代码

```cpp
#include <iostream>

int main() {
    double target{};
    double current{};
    // 读取、计算并输出
    return 0;
}
```

### 预期结果

```text
输入 30 22.5 时输出：Error: 7.5
```

### 验收标准

- [ ] 三个 `double` 对象都有明确初始状态
- [ ] 能指出哪一行是初始化、哪一行是赋值
- [ ] 不用未初始化值参与表达式

### 你的实现

```cpp

```

使用的构建命令：

```bash

```

实际输出：

```text

```

预测与实际的差异：

> 

---

## C｜调试任务

### 故障代码

```cpp
#include <iostream>

int main() {
    int total{5};
    int count{2};
    double average{total / count};
    std::cout << average << '\n';
}
```

### 调试要求

程序输出 2 而不是 2.5。说明问题不是语法错误，而是整数除法的类型规则；至少给出一种不丢失小数的修复。

运行或构建前的预测：

> 

错误发生的阶段：

> 

关键诊断或异常现象：

```text

```

根本原因：

> 

修复后的代码：

```cpp

```

为什么这个修复有效：

> 

---

## D｜求职面试口述

先用 30～60 秒回答：

> `double x = 3.0;` 中的等号为什么仍是初始化，不是赋值？

答案必须能够回到本课实验中的具体事实，例如：

- `double target_angle{30.0};`：创建 `double` 对象并初始化为 30.0
- `target_angle - current_angle`：表达式读取两个值并计算新值 7.5
- `double error{...};`：用表达式结果初始化另一个对象
- `target_angle = 45.0;`：对象已经存在，现在执行赋值

第一次口述提纲：

> 

面试追问：

> 类型除了决定占用空间，还影响什么？

你的回答：

> 

删掉任何只能背诵、不能由代码或诊断推导出的句子，再写第二版提纲：

> 

---

## 学习复盘

今天最清楚的机制：

> 

仍然不能通过代码解释的地方：

> 

本单元是否达到四类任务的验收标准：

> 
