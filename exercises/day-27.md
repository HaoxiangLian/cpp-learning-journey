# 第 27 天｜求职版练习

> 配套讲义：[模板、`auto` 与类型推导](../days/第27天-模板、auto与类型推导.md)

## 作答规则

- 先预测，再编译或运行；实际与预测不同就记录第一处差异。
- 所有 C++ 代码使用 C++17，并启用 `-Wall -Wextra -pedantic`；线程题额外使用 `-pthread`。
- 故意失败的程序要写清失败阶段、关键诊断、根因和验证证据。
- 每道题的答案直接写在题目下方。
- 四类任务全部达到验收标准，本单元才算完成。

---

## A｜基础概念检查

1. 模板定义为什么常放在头文件？

   > 

2. `auto` 会保留引用和顶层 `const` 吗，为什么要看具体写法？

   > 

3. 这个方案有什么限制，什么时候不该使用？

   > 

4. 用本课最小示例说明：哪个对象、文件或调用在什么语句前后发生了变化？

   > 

---

## B｜可运行编程任务

### 任务

补全函数模板 `clamp_value`，分别处理 `int` 和 `double`；再故意调用一次混合类型，记录推导诊断后修复。

### 起始代码

```cpp
#include <iostream>

template <typename T>
T clamp_value(T value, T low, T high) {
    // TODO
}

int main() {
    const auto a = clamp_value(12, 0, 10);
    const auto b = clamp_value(1.25, 0.0, 1.0);
    std::cout << a << "\n" << b << "\n";

    // 实验：取消下一行注释，观察冲突；随后统一实参类型
    // const auto c = clamp_value(1.25, 0, 1);
}
```

### 预期结果

```text
10
1
```

### 验收标准

- [ ] 两次有效调用分别形成 `int` 与 `double` 版本
- [ ] 记录混合实参导致的模板推导冲突
- [ ] 能解释 `auto` 变量推导后仍有固定静态类型

### 你的实现

```cpp

```

使用的构建命令：

```bash

```

运行前的输出预测：

> 

实际输出：

```text

```

预测与实际的第一处差异：

> 

---

## C｜调试任务

### 故障代码

```cpp
auto result = clamp_value(12.5, 0, 10);
```

### 调试要求

先不要修改。判断问题首先出现在编译、链接、运行、结果还是接口设计；运行或构建后，只保留最关键的诊断。修复时说明为什么最小修改能针对根因，而不是只让报错消失。

运行或构建前的预测：

> 

问题首先出现的阶段：

> 

关键诊断或异常现象：

```text

```

根本原因：

> 

修复后的代码：

```cpp

```

为什么这个修复有效：

> 

修复后的验证输入与结果：

```text

```

---

## D｜求职面试口述

先用 30～60 秒回答本课核心问题。回答必须包含：

- 能从两次调用写出 `T` 的具体类型
- 能制造并解释一次推导冲突
- 能区分模板、宏和运行时多态

参考结构是“具体问题 → 对象或文件怎样变化 → 使用的规则 → 一个限制”，不要堆术语。

第一次口述提纲：

> 

面试官追问：

> 模板定义为什么常放在头文件？

你的回答：

> 

再追问：

> `auto` 会保留引用和顶层 `const` 吗，为什么要看具体写法？

你的回答：

> 

删掉所有不能回到代码、输出、诊断或测试证据的句子，再写第二版提纲：

> 

---

## 学习复盘

今天最清楚的机制：

> 

仍然不能通过代码解释的地方：

> 

我能否在不看讲义时复现程序和故障：

> 

本单元是否达到四类任务的验收标准：

> 
