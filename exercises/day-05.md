# 第 5 天：函数的声明、定义、调用与返回｜求职版练习

## 作答规则

- 先预测，再编译或运行；实际结果与预测不同时，必须记录差异。
- 所有代码使用 C++17，并开启 `-Wall -Wextra -pedantic`。
- 故意失败的程序必须写明失败阶段、关键诊断和修复依据。
- 每道题的答案直接写在题目下方。
- 四类必做任务全部通过后，本单元才算完成。

---

## A｜基础概念检查

1. 声明、定义和调用各解决什么问题？

   > 

2. 形参与实参为什么不是同一个概念？

   > 

3. 结合本课最小代码，按顺序说明其中至少三个对象、值或文件发生了什么变化。

   > 

---

## B｜可运行编程任务

### 任务

声明、定义并调用 `double position_error(double target, double current)`，再根据误差绝对值输出是否需要减速。

### 起始代码

```cpp
#include <cmath>
#include <iostream>

// 写函数声明

int main() {
    double error{/* 调用函数 */};
    std::cout << error << '\n';
    return 0;
}

// 写函数定义
```

### 预期结果

```text
目标 30、当前 22.5 时先输出 `7.5`
```

### 验收标准

- [ ] 调用点之前存在可见声明
- [ ] 定义的函数签名与声明完全一致
- [ ] 能指出实参、形参和返回值

### 你的实现

```cpp

```

使用的构建命令：

```bash

```

实际输出：

```text

```

预测与实际的差异：

> 

---

## C｜调试任务

### 故障代码

```cpp
double position_error(double target, double current);

int main() {
    return position_error(30.0, 22.5) > 5.0;
}
```

### 调试要求

这段代码可以生成目标文件，但若没有函数定义会链接失败。实际制造错误，记录 `undefined reference`，再补上定义。

运行或构建前的预测：

> 

错误发生的阶段：

> 

关键诊断或异常现象：

```text

```

根本原因：

> 

修复后的代码：

```cpp

```

为什么这个修复有效：

> 

---

## D｜求职面试口述

先用 30～60 秒回答：

> 声明、定义和调用各解决什么问题？

答案必须能够回到本课实验中的具体事实，例如：

- 声明让调用点知道函数名、参数类型和返回类型
- 调用表达式用两个实参初始化两个形参对象
- 控制转入函数体并计算 `target - current`
- 返回值回到调用点，用于初始化 `error`

第一次口述提纲：

> 

面试追问：

> 形参与实参为什么不是同一个概念？

你的回答：

> 

删掉任何只能背诵、不能由代码或诊断推导出的句子，再写第二版提纲：

> 

---

## 学习复盘

今天最清楚的机制：

> 

仍然不能通过代码解释的地方：

> 

本单元是否达到四类任务的验收标准：

> 
