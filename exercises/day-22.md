# 第 22 天练习：移动语义、右值引用与 `std::move`

## 练习 1：值类别与引用绑定

判断下列表达式是 lvalue、xvalue 还是 prvalue，并说明能否绑定到 `IntBuffer&&`。

```cpp
buffer
std::move(buffer)
IntBuffer{3}
make_buffer()
```

>
>
>
>
>

## 练习 2：`std::move` 做了什么

解释为什么 `std::move` 本身不移动资源。指出资源真正在哪个函数中发生转移。

>
>
>
>

## 练习 3：实现移动构造

为拥有动态数组的类写移动构造函数，转移大小和指针，并把源对象恢复为可析构、可赋值的状态。

```cpp

```

## 练习 4：实现移动赋值

写出自移动安全的移动赋值运算符。说明目标旧资源、源资源和源对象分别发生什么。

```cpp

```

>
>
>

## 练习 5：预测主示例

阅读 `main.cpp`，预测移动后三个对象的大小及输出，再运行并记录实际结果。

### 预测

>
>
>

### 实际结果

>
>
>
>

## 练习 6：被移动对象

说明“有效但未指定状态”允许哪些操作、不允许假定什么。为什么本课程自定义 `IntBuffer` 可以额外保证移动后 `size() == 0`？

>
>
>
>

## 练习 7：从 const 对象移动

预测 `const_move_copies.cpp` 输出 `copy` 还是 `move`，说明 `std::move(source)` 的类型以及重载决议为何选择该构造函数。

### 预测

>
>

### 实际结果

>
>
>

## 练习 8：错误移动实验

画出 `bad_move_error.cpp` 移动后两个指针的关系，预测析构阶段的问题，再使用 ASan 验证。

### 预测

>
>
>

### 实际结果

>
>
>

## 练习 9：`noexcept` 与容器

解释为什么标准容器重新分配时更愿意使用可证明不抛异常的移动构造；为什么不能为了性能把可能抛异常的移动函数谎报为 `noexcept`？

>
>
>
>

## 练习 10：Rule of Five 与 Rule of Zero

分别为“直接拥有裸资源的教学类”和“只含 `std::vector<int>` 的业务类”选择 Rule of Five 或 Rule of Zero，并说明理由。

>
>
>
>

## 完成核对

- [ ] 已区分 lvalue、xvalue、prvalue 与右值引用
- [ ] 已说明 `std::move` 只是转换
- [ ] 已实现移动构造和移动赋值
- [ ] 已保持被移动对象可析构、可赋值
- [ ] 已验证 const 对象通常不能调用普通移动构造
- [ ] 已复现错误移动导致的重复释放
- [ ] 已解释 `noexcept`、Rule of Five 与 Rule of Zero
