# 第 22 天：移动语义、右值引用与 `std::move`｜求职版练习

## 作答规则

- 先预测，再编译或运行；实际结果与预测不同时，必须记录差异。
- 所有代码使用 C++17，并开启 `-Wall -Wextra -pedantic`。
- 故意失败的程序必须写明失败阶段、关键诊断和修复依据。
- 每道题的答案直接写在题目下方。
- 四类必做任务全部通过后，本单元才算完成。

---

## A｜基础概念检查

1. 为什么从 `const` 对象使用 `std::move` 往往仍然复制？

   > 

2. `noexcept` 为什么会影响 `vector` 重分配策略？

   > 

3. 结合本课最小代码，按顺序说明其中至少三个对象、值或文件发生了什么变化。

   > 

---

## B｜可运行编程任务

### 任务

比较一个大型 `std::vector<double>` 的复制构造与移动构造，并只依赖标准保证判断来源对象后续可做什么。

### 起始代码

```cpp
#include <iostream>
#include <utility>
#include <vector>

int main() {
    std::vector<double> source(1000, 1.0);
    std::vector<double> copied{source};
    std::vector<double> moved{std::move(source)};
    // 输出 copied 和 moved 的大小；不要假定 source 的具体大小
}
```

### 预期结果

```text
`copied` 和 `moved` 都有 1000 个元素；来源对象仍可清空、赋值和析构
```

### 验收标准

- [ ] 不把 `std::move` 描述为执行搬运
- [ ] 不依赖移动后 `source` 的未保证具体值
- [ ] 能说明真正移动由构造函数完成

### 你的实现

```cpp

```

使用的构建命令：

```bash

```

实际输出：

```text

```

预测与实际的差异：

> 

---

## C｜调试任务

### 故障代码

```cpp
const std::vector<int> source{1, 2, 3};
std::vector<int> target{std::move(source)};
```

### 调试要求

这通常选择复制而非移动。根据 `const` 来源与移动构造参数类型解释原因，不要只说“编译器没优化”。

运行或构建前的预测：

> 

错误发生的阶段：

> 

关键诊断或异常现象：

```text

```

根本原因：

> 

修复后的代码：

```cpp

```

为什么这个修复有效：

> 

---

## D｜求职面试口述

先用 30～60 秒回答：

> 为什么从 `const` 对象使用 `std::move` 往往仍然复制？

答案必须能够回到本课实验中的具体事实，例如：

- `std::move(source)` 只是把表达式转换成可绑定右值引用的形式
- 重载决议选择 `vector` 的移动构造函数
- 目标对象接管或以类型允许的方式取得资源
- 来源对象仍可析构和重新赋值；其他状态遵守类型保证

第一次口述提纲：

> 

面试追问：

> `noexcept` 为什么会影响 `vector` 重分配策略？

你的回答：

> 

删掉任何只能背诵、不能由代码或诊断推导出的句子，再写第二版提纲：

> 

---

## 学习复盘

今天最清楚的机制：

> 

仍然不能通过代码解释的地方：

> 

本单元是否达到四类任务的验收标准：

> 
