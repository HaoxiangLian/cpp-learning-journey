# 第 18 天：警告、调试器、Sanitizer 与错误分类｜求职版练习

## 作答规则

- 先预测，再编译或运行；实际结果与预测不同时，必须记录差异。
- 所有代码使用 C++17，并开启 `-Wall -Wextra -pedantic`。
- 故意失败的程序必须写明失败阶段、关键诊断和修复依据。
- 每道题的答案直接写在题目下方。
- 四类必做任务全部通过后，本单元才算完成。

---

## A｜基础概念检查

1. 为什么“零警告”不等于程序没有错误？

   > 

2. ASan、UBSan 和调试器分别最适合解决什么问题？

   > 

3. 结合本课最小代码，按顺序说明其中至少三个对象、值或文件发生了什么变化。

   > 

---

## B｜可运行编程任务

### 任务

对一个越界程序依次使用严格警告、调试构建、GDB 和 ASan，写出每种工具提供了什么证据。

### 起始代码

```cpp
#include <iostream>
#include <vector>

int main() {
    std::vector<int> values{1, 2, 3};
    for (std::size_t i{0}; i <= values.size(); ++i) {
        std::cout << values[i] << '\n';
    }
}
```

### 预期结果

```text
ASan 指出越界访问；修复后输出 1、2、3 且不再报告
```

### 验收标准

- [ ] 记录完整构建命令
- [ ] 区分警告、调试器和 ASan 的能力
- [ ] 增加一个边界测试防止回归

### 你的实现

```cpp

```

使用的构建命令：

```bash

```

实际输出：

```text

```

预测与实际的差异：

> 

---

## C｜调试任务

### 故障代码

```cpp
int main() {
    int value;
    return value > 0 ? 0 : 1;
}
```

### 调试要求

开启严格警告记录诊断，再初始化变量。说明警告为什么不是语言规则的完整证明。

运行或构建前的预测：

> 

错误发生的阶段：

> 

关键诊断或异常现象：

```text

```

根本原因：

> 

修复后的代码：

```cpp

```

为什么这个修复有效：

> 

---

## D｜求职面试口述

先用 30～60 秒回答：

> 为什么“零警告”不等于程序没有错误？

答案必须能够回到本课实验中的具体事实，例如：

- 编译器可接受语法，严格警告不一定证明程序安全
- 程序运行到 `i == size()` 时越界
- 使用 `-g -Og` 便于调试器对应源码
- 使用 AddressSanitizer 重新构建，本次执行会检测许多越界与失效内存访问

第一次口述提纲：

> 

面试追问：

> ASan、UBSan 和调试器分别最适合解决什么问题？

你的回答：

> 

删掉任何只能背诵、不能由代码或诊断推导出的句子，再写第二版提纲：

> 

---

## 学习复盘

今天最清楚的机制：

> 

仍然不能通过代码解释的地方：

> 

本单元是否达到四类任务的验收标准：

> 
