# 第 18 天练习：警告、调试器、Sanitizer 与错误分类

## 练习 1：按阶段分类

分别判断以下问题最先在哪个阶段暴露，并写出依据：

1. `#include "missing.hpp"` 找不到文件。
2. 调用了只有声明、没有定义的 `read_force()`。
3. 可执行文件依赖的共享库在运行机器上不存在。
4. `vector` 下标越界，但程序已经启动。
5. 程序输出 12，而需求规定应输出 21。

>
>
>
>
>
>

## 练习 2：诊断与合法性

解释为什么“零警告构建成功”不能证明程序符合全部 C++ 规则，也不能证明业务逻辑正确。回答中使用“无需诊断规则”和“逻辑错误”。

>
>
>
>

## 练习 3：警告选项

分别解释 `-Wall`、`-Wextra`、`-Wpedantic`、`-Werror`。`-Wall` 是否表示 GCC 的所有警告？

>
>
>
>
>

## 练习 4：预测未初始化读取实验

阅读 `examples/day-18/warning_uninitialized.cpp`，判断 `value` 经历了什么初始化，以及读取它在 C++17 中属于什么行为。再使用讲义命令编译，记录编译器的实际诊断。不要运行该程序并依赖未定义行为产生的具体数字。

### 预测

>
>
>

### 实际结果

>
>
>
>

## 练习 5：编写安全修复

修改下面函数，使所有返回路径都拥有由程序逻辑明确产生的值；不要只为了压制警告随意填入一个没有需求含义的默认值。

```cpp

```

## 练习 6：调试器命令

针对 `debug_target.cpp` 写出一组 GDB 命令，完成以下操作：在 `weighted_sum` 暂停、运行程序、查看参数和局部变量、单步一次、打印 `total`、查看调用栈、切换到调用者栈帧、继续运行。

```text








```

## 练习 7：预测 ASan 实验

`values` 有 3 个元素。预测 `values[values.size()]` 对应的下标、合法下标范围和 ASan 错误类别，再运行实验并记录首个指向用户源码的栈帧。

### 预测

>
>
>

### 实际结果

>
>
>
>

## 练习 8：预测 UBSan 实验

预测 `std::numeric_limits<int>::max() + 1` 在 C++ 中属于什么行为。不要把常见机器上的回绕结果当作语言保证。运行 UBSan 实验并记录诊断类别。

### 预测

>
>
>

### 实际结果

>
>
>

## 练习 9：工具选择

为下列问题选择主要工具，并说明工具边界：

- 自动局部 `int` 可能未初始化。
- 堆对象释放后仍被访问。
- 有符号整数溢出。
- 算法把加法写成减法。
- 两个 `.cpp` 重复定义同一普通外部函数。

>
>
>
>
>
>

## 练习 10：写一个断言

编写函数 `double average(int sum, int count)`，用断言表达内部前置条件 `count > 0`。再解释为什么不能把具有必要副作用的函数调用放进 `assert`。

```cpp

```

>
>
>

## 完成核对

- [ ] 已区分构建各阶段、运行时问题和逻辑错误
- [ ] 已保留并分析编译器警告，而不是机械消音
- [ ] 已完成一次 GDB 命令流程或准确记录当前环境缺少 GDB
- [ ] 已运行 ASan 和 UBSan 故意错误实验
- [ ] 已说明动态分析只检查本次执行路径
- [ ] 已保留预测和实际结果，不用后者覆盖前者
