# 第 20 天练习：析构函数与 RAII

## 练习 1：完整生命周期

以 `IntBuffer samples{3};` 为例，按顺序说明存储取得、成员初始化、构造函数体、正常使用、析构函数体、成员销毁、存储期结束之间的关系。

>
>
>
>
>
>

## 练习 2：预测销毁顺序

阅读 `examples/day-20/destruction_order.cpp`，先写出完整输出顺序，再运行验证。解释为什么成员销毁顺序与成员声明顺序相反。

### 预测

>
>
>
>

### 实际结果

>
>
>
>

## 练习 3：构造失败

阅读 `constructor_failure.cpp`，预测 `Controller` 构造函数体抛出异常后，哪些析构函数会执行、顺序如何、哪个析构函数不会执行，并说明原因。

### 预测

>
>
>
>

### 实际结果

>
>
>
>

## 练习 4：RAII 与手动清理

解释为什么把 `delete[]` 只写在普通函数末尾无法覆盖提前 `return`、异常和后续代码修改，而将它绑定到析构函数后可以覆盖正常作用域退出与栈展开。

>
>
>
>
>

## 练习 5：实现一个不可复制的资源类

编写 `FileHandle` 的类声明：构造函数接收资源，析构函数释放资源，并删除复制构造与复制赋值。资源的具体系统调用可以只声明为普通函数，不要用伪代码冒充 C++。

```cpp

```

## 练习 6：Rule of Zero

设计一个只包含 `std::string` 和 `std::vector<int>` 成员的 `LogBook`。不要声明析构函数、复制或移动操作，并解释这些成员为什么能自动管理自身资源。

```cpp

```

>
>
>

## 练习 7：显式析构错误实验

预测 `explicit_destructor_error.cpp` 为什么会对同一资源执行两次释放，再使用 ASan 运行并记录错误类别和首个用户源码位置。

### 预测

>
>
>

### 实际结果

>
>
>
>

## 练习 8：析构函数与异常

解释为什么析构函数通常不应让异常传播。说明在另一个异常已经触发栈展开时，析构函数再次抛出会造成什么后果。

>
>
>
>

## 练习 9：自动对象与动态对象

对比以下两种对象的析构触发方式与存储释放责任：

```cpp
IntBuffer local{3};
IntBuffer* dynamic{new IntBuffer{3}};
```

>
>
>
>
>

## 练习 10：静态对象销毁顺序

解释为什么一个 `.cpp` 中静态对象的析构函数不应依赖另一个 `.cpp` 中可能已经销毁的静态对象。写出一个减少该依赖的函数局部静态访问接口，并指出它仍需考虑什么销毁问题。

>
>
>
>

```cpp

```

## 完成核对

- [ ] 已区分析构函数体、成员析构和存储释放
- [ ] 已验证成员按构造的相反顺序销毁
- [ ] 已解释构造失败时已构造成员如何清理
- [ ] 已解释 RAII 如何覆盖正常退出与栈展开
- [ ] 已说明资源所有者为什么不能盲目浅复制
- [ ] 已理解 Rule of Zero 的适用方向
- [ ] 已识别静态对象销毁顺序风险
