# 第 16 天：汇编、目标文件、符号、重定位、链接与库｜求职版练习

## 作答规则

- 先预测，再编译或运行；实际结果与预测不同时，必须记录差异。
- 所有代码使用 C++17，并开启 `-Wall -Wextra -pedantic`。
- 故意失败的程序必须写明失败阶段、关键诊断和修复依据。
- 每道题的答案直接写在题目下方。
- 四类必做任务全部通过后，本单元才算完成。

---

## A｜基础概念检查

1. 目标文件已经有机器指令，为什么通常还不能直接运行？

   > 

2. 符号与重定位分别解决什么问题？

   > 

3. 结合本课最小代码，按顺序说明其中至少三个对象、值或文件发生了什么变化。

   > 

---

## B｜可运行编程任务

### 任务

创建 `main.cpp` 和 `force.cpp`，分别生成 `.o`，用 `nm -C` 观察同一函数在两个目标文件中的未定义与已定义状态，再完成链接。

### 起始代码

```cpp
// main.cpp
void report_force(double);
int main() { report_force(3.2); }

// force.cpp
#include <iostream>
void report_force(double value) { std::cout << value << '\n'; }
```

### 预期结果

```text
最终程序输出 `3.2`；`main.o` 对函数显示未定义需求，`force.o` 提供定义
```

### 验收标准

- [ ] 保存两个目标文件
- [ ] 实际制造一次漏掉 `force.o` 的链接失败
- [ ] 用自己的输出解释符号与重定位

### 你的实现

```cpp

```

使用的构建命令：

```bash

```

实际输出：

```text

```

预测与实际的差异：

> 

---

## C｜调试任务

### 故障代码

```cpp
g++ -std=c++17 -c main.cpp -o main.o
g++ main.o -o app
```

### 调试要求

这是命令级调试题：说明为什么第一条成功而第二条失败；补全正确的分离编译和链接命令。

运行或构建前的预测：

> 

错误发生的阶段：

> 

关键诊断或异常现象：

```text

```

根本原因：

> 

修复后的代码：

```cpp

```

为什么这个修复有效：

> 

---

## D｜求职面试口述

先用 30～60 秒回答：

> 目标文件已经有机器指令，为什么通常还不能直接运行？

答案必须能够回到本课实验中的具体事实，例如：

- 两个 `.cpp` 分别预处理、编译和汇编，得到两个目标文件
- `main.o` 记录对 `report_force(double)` 的未定义符号需求
- `force.o` 提供该函数的已定义符号
- 调用地址未确定的位置带有重定位记录

第一次口述提纲：

> 

面试追问：

> 符号与重定位分别解决什么问题？

你的回答：

> 

删掉任何只能背诵、不能由代码或诊断推导出的句子，再写第二版提纲：

> 

---

## 学习复盘

今天最清楚的机制：

> 

仍然不能通过代码解释的地方：

> 

本单元是否达到四类任务的验收标准：

> 
