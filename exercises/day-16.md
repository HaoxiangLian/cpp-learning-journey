# 第 16 天练习：汇编、目标文件、符号、重定位、链接与库

## 练习 1：完整链路

从两个 `.cpp` 开始画出各自的预处理、翻译单元、汇编、目标文件以及最终链接、装载和进入 `main` 的完整过程。

>
>
>
>
>

## 练习 2：汇编与目标文件

分别生成 `force_model.s` 和 `force_model.o`，记录文件类型、大小，并说明为什么两者不能视为同一产物。

>
>
>
>

## 练习 3：符号预测与验证

在运行 `nm -C` 前，预测 `main.o` 和 `force_model.o` 中 `main`、`compute_force(double)` 的状态，再实际检查。

### 预测

>
>
>

### 实际结果

>
>
>

## 练习 4：重定位项

用 `readelf -rW` 和 `objdump -drC` 查找调用 `compute_force` 相关记录，解释待修正位置、符号和重定位类型各表示什么。

>
>
>
>

## 练习 5：未定义引用

只链接 `main.o`，记录关键诊断，并说明此前哪些阶段已经成功、失败发生在哪一阶段。

>
>
>
>

## 练习 6：重复定义

加入 `duplicate_force_model.o`，记录诊断，并比较它与“同一翻译单元重定义”错误的发现阶段。

>
>
>
>

## 练习 7：静态库

创建 `libforce_model.a`，列出成员并链接程序。说明为什么使用 `.a` 不等于整个程序完全静态。

```bash

```

>
>

## 练习 8：共享库

创建 PIC 目标文件和 `.so`，链接运行程序并用 `readelf -dW` 查找 `NEEDED` 项。

```bash

```

>
>

## 练习 9：装载失败预测与验证

临时把共享库移出搜索路径，预测程序表现并运行；随后恢复库和正确路径。

### 预测

>
>

### 实际结果

>
>

## 练习 10：层次分类

将以下概念分别归入 C++ 语言、工具链、ELF/ABI 或操作系统层：翻译单元、`.text`、名称修饰、`nm`、链接器、装载器、ODR、`RPATH`。

>
>
>
>
>

## 完成检查

- [ ] 我实际生成并检查了 `.s` 与 `.o`
- [ ] 我检查了符号表和重定位项
- [ ] 我复现了两类链接错误
- [ ] 我创建并运行了静态库版本
- [ ] 我创建并运行了共享库版本
- [ ] 我能区分 C++、工具链、ABI 与操作系统规则
