# 第 14 天：表达式求值、顺序、值类别与未定义行为｜求职版练习

## 作答规则

- 先预测，再编译或运行；实际结果与预测不同时，必须记录差异。
- 所有代码使用 C++17，并开启 `-Wall -Wextra -pedantic`。
- 故意失败的程序必须写明失败阶段、关键诊断和修复依据。
- 每道题的答案直接写在题目下方。
- 四类必做任务全部通过后，本单元才算完成。

---

## A｜基础概念检查

1. 为什么 `&&` 可以用于先检查指针再解引用？

   > 

2. 未定义行为为什么不能理解为“结果不确定但程序仍会正常继续”？

   > 

3. 结合本课最小代码，按顺序说明其中至少三个对象、值或文件发生了什么变化。

   > 

---

## B｜可运行编程任务

### 任务

安全查找第一个正采样值：先证明下标合法，再访问元素，并记录每次条件求值。

### 起始代码

```cpp
#include <iostream>
#include <vector>

int main() {
    std::vector<int> samples{0, 0, 5};
    std::size_t i{0};
    while (i < samples.size() && samples[i] <= 0) {
        ++i;
    }
    // 安全输出找到或未找到
}
```

### 预期结果

```text
Found: 5
```

### 验收标准

- [ ] 任何 `samples[i]` 前都能证明 `i < size()`
- [ ] 能解释 `&&` 的短路顺序
- [ ] 未找到时不使用尾后下标

### 你的实现

```cpp

```

使用的构建命令：

```bash

```

实际输出：

```text

```

预测与实际的差异：

> 

---

## C｜调试任务

### 故障代码

```cpp
int values[3]{1, 2, 3};
for (int i{0}; i <= 3; ++i) {
    values[i] *= 2;
}
```

### 调试要求

判断这是编译错误还是运行期未定义行为；使用 ASan 获取证据并修复。

运行或构建前的预测：

> 

错误发生的阶段：

> 

关键诊断或异常现象：

```text

```

根本原因：

> 

修复后的代码：

```cpp

```

为什么这个修复有效：

> 

---

## D｜求职面试口述

先用 30～60 秒回答：

> 为什么 `&&` 可以用于先检查指针再解引用？

答案必须能够回到本课实验中的具体事实，例如：

- 先计算 `i < data.size()`
- 由于左侧为真，`&&` 才计算右侧 `data[i] > 0`
- 条件整体产生 `bool` 值
- 进入代码块后，`++i` 修改对象，这是副作用

第一次口述提纲：

> 

面试追问：

> 未定义行为为什么不能理解为“结果不确定但程序仍会正常继续”？

你的回答：

> 

删掉任何只能背诵、不能由代码或诊断推导出的句子，再写第二版提纲：

> 

---

## 学习复盘

今天最清楚的机制：

> 

仍然不能通过代码解释的地方：

> 

本单元是否达到四类任务的验收标准：

> 
