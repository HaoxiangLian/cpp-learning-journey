# 第 7 天：原生数组、字符串类与向量容器｜求职版练习

## 作答规则

- 先预测，再编译或运行；实际结果与预测不同时，必须记录差异。
- 所有代码使用 C++17，并开启 `-Wall -Wextra -pedantic`。
- 故意失败的程序必须写明失败阶段、关键诊断和修复依据。
- 每道题的答案直接写在题目下方。
- 四类必做任务全部通过后，本单元才算完成。

---

## A｜基础概念检查

1. 数组与指针为什么不是同一种类型？

   > 

2. `vector` 扩容后，旧元素引用为什么可能失效？

   > 

3. 结合本课最小代码，按顺序说明其中至少三个对象、值或文件发生了什么变化。

   > 

---

## B｜可运行编程任务

### 任务

编写 `average` 和 `max_abs_angle`，参数使用 `const std::vector<double>&`；空序列时安全返回。

### 起始代码

```cpp
#include <cmath>
#include <iostream>
#include <vector>

// 编写两个函数

int main() {
    std::vector<double> angles{10.0, -30.0, 20.0};
    // 输出平均值和最大绝对角度
}
```

### 预期结果

```text
Average: 0
Max abs: 30
```

### 验收标准

- [ ] 不越界
- [ ] 不复制整个 `vector`
- [ ] 空序列有明确行为
- [ ] 使用范围 `for` 或合法下标

### 你的实现

```cpp

```

使用的构建命令：

```bash

```

实际输出：

```text

```

预测与实际的差异：

> 

---

## C｜调试任务

### 故障代码

```cpp
#include <vector>

int main() {
    std::vector<int> values{1, 2, 3};
    for (std::size_t i{0}; i <= values.size(); ++i) {
        values[i] *= 2;
    }
}
```

### 调试要求

使用 ASan 观察 `i == size()` 时的越界，并修复循环条件。说明 `operator[]` 为什么没有主动报异常。

运行或构建前的预测：

> 

错误发生的阶段：

> 

关键诊断或异常现象：

```text

```

根本原因：

> 

修复后的代码：

```cpp

```

为什么这个修复有效：

> 

---

## D｜求职面试口述

先用 30～60 秒回答：

> 数组与指针为什么不是同一种类型？

答案必须能够回到本课实验中的具体事实，例如：

- `vector<double>` 拥有一段连续元素序列，并记录大小
- 范围 `for` 每轮把一个元素值复制到 `angle`
- `size()` 给出当前元素数量；合法下标为 0 到 `size()-1`
- `operator[]` 不检查边界，`at()` 会检查并通过异常报告

第一次口述提纲：

> 

面试追问：

> `vector` 扩容后，旧元素引用为什么可能失效？

你的回答：

> 

删掉任何只能背诵、不能由代码或诊断推导出的句子，再写第二版提纲：

> 

---

## 学习复盘

今天最清楚的机制：

> 

仍然不能通过代码解释的地方：

> 

本单元是否达到四类任务的验收标准：

> 
