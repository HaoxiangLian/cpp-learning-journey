# 第 36 天｜求职版练习

> 配套讲义：[机器人 C++ 综合项目与模拟面试](../days/第36天-机器人C++综合项目与模拟面试.md)

## 作答规则

- 先预测，再编译或运行；实际与预测不同就记录第一处差异。
- 所有 C++ 代码使用 C++17，并启用 `-Wall -Wextra -pedantic`；线程题额外使用 `-pthread`。
- 故意失败的程序要写清失败阶段、关键诊断、根因和验证证据。
- 每道题的答案直接写在题目下方。
- 四类任务全部达到验收标准，本单元才算完成。

---

## A｜基础概念检查

1. 请用 60 秒讲清数据从输入到输出的路径。

   > 

2. 为什么计数用 unordered_map、结果用 vector？

   > 

3. 若接入实时采集线程，当前设计首先要补哪些约束？

   > 

4. 用本课最小示例说明：哪个对象、文件或调用在什么语句前后发生了变化？

   > 

---

## B｜可运行编程任务

### 任务

把综合示例拆成库和应用，加入文件输入、错误行号、至少 6 个自动测试，并写一份 3 分钟项目介绍提纲。

### 起始代码

```cpp
// 建议边界（可根据证据调整）：
// include/telemetry/sample.hpp
// include/telemetry/pipeline.hpp
// src/pipeline.cpp
// app/main.cpp
// tests/pipeline_test.cpp
// CMakeLists.txt
//
// TODO: 定义输入契约、校验结果、统计结果和失败表示。
```

### 预期结果

```text
从干净构建目录配置、构建、测试全部通过；示例输入能输出有效数、拒绝行及每个关节计数。
```

### 验收标准

- [ ] README 给出一键构建、测试和运行命令
- [ ] 容器、所有权和错误策略都能用具体约束解释
- [ ] 测试覆盖空输入、边界、坏行和正常路径
- [ ] 3 分钟介绍包含问题、数据流、关键取舍、验证证据和下一步

### 你的实现

```cpp

```

使用的构建命令：

```bash

```

运行前的输出预测：

> 

实际输出：

```text

```

预测与实际的第一处差异：

> 

---

## C｜调试任务

### 故障代码

```cpp
#include <numeric>
#include <vector>

double average(const std::vector<double>& values) {
    const double sum = std::accumulate(values.begin(), values.end(), 0.0);
    return sum / values.size();
}

// 空输入时 size() 为 0，接口没有定义失败方式。
```

### 调试要求

先不要修改。判断问题首先出现在编译、链接、运行、结果还是接口设计；运行或构建后，只保留最关键的诊断。修复时说明为什么最小修改能针对根因，而不是只让报错消失。

运行或构建前的预测：

> 

问题首先出现的阶段：

> 

关键诊断或异常现象：

```text

```

根本原因：

> 

修复后的代码：

```cpp

```

为什么这个修复有效：

> 

修复后的验证输入与结果：

```text

```

---

## D｜求职面试口述

先用 30～60 秒回答本课核心问题。回答必须包含：

- 能从干净目录构建、运行并复现输出
- 至少提交边界测试和一个故障修复记录
- 能在 3 分钟内用具体代码证据介绍项目

参考结构是“具体问题 → 对象或文件怎样变化 → 使用的规则 → 一个限制”，不要堆术语。

第一次口述提纲：

> 

面试官追问：

> 请用 60 秒讲清数据从输入到输出的路径。

你的回答：

> 

再追问：

> 为什么计数用 unordered_map、结果用 vector？

你的回答：

> 

删掉所有不能回到代码、输出、诊断或测试证据的句子，再写第二版提纲：

> 

---

## 学习复盘

今天最清楚的机制：

> 

仍然不能通过代码解释的地方：

> 

我能否在不看讲义时复现程序和故障：

> 

本单元是否达到四类任务的验收标准：

> 
