# 第 15 天练习：预处理、头文件、宏与翻译单元

## 练习 1：完整过程定位

从主源文件开始写出完整构建链路，并标出 `#include`、宏展开、语法/类型检查、生成目标文件和解析符号分别发生在哪一阶段。

>
>
>
>
>

## 练习 2：观察 `.ii`

生成 `main.cpp` 的预处理结果，记录文件大小，并定位 `status_text` 声明和 `main` 定义。

>
>
>
>

## 练习 3：翻译单元边界

画出 `main.cpp` 与 `robot_status.cpp` 各自递归包含的内容，并回答最终有几个翻译单元、几个目标文件。

>
>
>
>
>

## 练习 4：编写自包含头

创建声明 `double clamp_force(double value, double limit);` 的自包含头文件，加入唯一包含保护。

```cpp

```

## 练习 5：重复包含预测与验证

预测连续两次包含无保护头的结果，再实际编译并记录错误发生阶段和关键诊断。

### 预测

>
>
>

### 实际结果

>
>
>

## 练习 6：宏展开预测

不运行程序，先写出每个表达式的完整展开及结果，再查看 `.ii` 验证。

```cpp
#define BAD_SQUARE(x) x * x
#define PAREN_SQUARE(x) ((x) * (x))

int a{BAD_SQUARE(1 + 2)};
int b{PAREN_SQUARE(1 + 2)};
```

### 预测

>
>
>

### 实际结果

>
>
>

## 练习 7：重复求值分析

解释 `PAREN_SQUARE(value++)` 为什么即使括号完整仍不安全，并用一个 `constexpr` 函数替代。

```cpp

```

## 练习 8：条件包含

写一个程序：只有定义 `ENABLE_TRACE` 时才输出调试信息。分别给出启用和禁用版本的构建命令。

```cpp

```

>
>

## 练习 9：声明存在但定义缺失

只构建并链接 `main.cpp`，记录为什么预处理和狭义编译可以完成，而链接失败。

>
>
>
>

## 练习 10：头文件审查

列出一个工程头文件应满足的至少五项检查：包含保护、自包含性、声明/定义边界、依赖和宏污染等。

>
>
>
>
>

## 完成检查

- [ ] 我生成并检查了 `.ii`
- [ ] 我能画出两个独立翻译单元
- [ ] 我复现了重复包含的编译错误
- [ ] 我复现了缺少定义的链接错误
- [ ] 我能写自包含且受保护的头文件
- [ ] 我能解释宏的优先级和重复求值风险
