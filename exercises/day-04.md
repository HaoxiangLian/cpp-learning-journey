# 第 4 天练习单

主题：源码到可执行文件的完整链路

## 作答方式

- 文字题直接在题目下方的空白处填写
- 命令题在紧随题目的代码块中填写或修改
- 观察题先预测，再执行命令并记录实际结果
- 错误实验必须记录错误发生的阶段和关键诊断
- 完成后保存并同步到 GitHub

---

## 练习 1：还原完整链路

不要看讲义，按先后顺序填写缺失阶段：

```text
源文件
  ↓

  ↓
预处理后的翻译单元
  ↓

  ↓
汇编文本
  ↓

  ↓
可重定位目标文件
  ↓

  ↓
可执行文件
  ↓

  ↓
运行库启动代码
  ↓
main
```

**你的完整链路：**

```text

```

为什么“一条 `g++` 命令没有留下 `.ii` 和 `.s` 文件”不等于这些阶段没有发生？

> 

---

## 练习 2：区分参与者的职责

分别用一到两句话说明：

1. 编译器驱动程序：

   > 

2. 预处理器：

   > 

3. 狭义编译器：

   > 

4. 汇编器：

   > 

5. 链接器：

   > 

6. 操作系统装载器：

   > 

`g++` 在今天的命令中主要扮演哪一种角色？

> 

---

## 练习 3：判断命令停在哪里

填写每条命令的最后阶段、主要产物以及是否进行链接。

| 命令 | 最后阶段 | 主要产物 | 是否链接 |
|---|---|---|---|
| `g++ -E a.cpp -o a.ii` |  |  |  |
| `g++ -S a.cpp -o a.s` |  |  |  |
| `g++ -c a.cpp -o a.o` |  |  |  |
| `g++ a.cpp -o a` |  |  |  |
| `g++ a.o -o a` |  |  |  |

为什么对 `.cpp` 使用 `-c` 不等于“只执行汇编”？

> 

---

## 练习 4：观察预处理结果

先阅读：

```cpp
#include <iostream>

#define ROBOT_NAME "Arm-A"

int main() {
    std::cout << "Robot: " << ROBOT_NAME << '\n';
    return 0;
}
```

### 4.1 预测

预处理后的文件与原文件相比，大小会怎样变化？为什么？

> 

预处理结果中，标识符 `ROBOT_NAME` 是否还会出现在使用位置？

> 

字符串 `Arm-A` 是否会出现在预处理结果中？

> 

### 4.2 执行

建立目录并写出生成 `pipeline.ii` 的命令：

```bash

```

### 4.3 实际观察

原始 `pipeline.cpp` 的大小：

> 

生成的 `pipeline.ii` 的大小：

> 

搜索 `ROBOT_NAME` 后看到的结果：

```text

```

搜索 `Arm-A` 后看到的关键位置：

```text

```

预测与实际是否一致？如果不一致，差异是什么？

> 

---

## 练习 5：生成并辨认四种产物

依次写出命令，生成：

1. `build/day-04/pipeline.ii`
2. `build/day-04/pipeline.s`
3. `build/day-04/pipeline.o`
4. `build/day-04/pipeline` 或 `pipeline.exe`

```bash

```

完成后填写：

| 文件 | 文本还是二进制 | 能否直接作为正常程序运行 | 你观察到的一个特征 |
|---|---|---|---|
| `pipeline.cpp` |  |  |  |
| `pipeline.ii` |  |  |  |
| `pipeline.s` |  |  |  |
| `pipeline.o` |  |  |  |
| `pipeline` / `pipeline.exe` |  |  |  |

运行最终程序后的实际输出：

```text

```

尝试直接运行 `pipeline.o` 后发生了什么？

```text

```

为什么目标文件通常不能直接运行？

> 

---

## 练习 6：观察符号和重定位

如果当前工具链提供 `nm`，执行：

```bash
nm -C build/day-04/pipeline.o
```

记录一个当前目标文件中已经定义的符号：

```text

```

记录一个需要从其他目标文件或库解析的未定义符号：

```text

```

如果提供 `objdump`，执行：

```bash
objdump -r build/day-04/pipeline.o
```

是否看到了重定位记录？

> 

用自己的话解释“符号”：

> 

用自己的话解释“重定位”：

> 

如果系统没有 `nm` 或 `objdump`，记录工具提示：

```text

```

---

## 练习 7：制造并修复链接错误

先分别生成两个目标文件：

```bash

```

### 7.1 故意失败

只使用 `link_main.o` 进行链接：

```bash

```

关键错误信息：

```text

```

这个错误属于哪个阶段？

> 

为什么 `link_main.cpp` 能生成目标文件，最后却不能生成可执行文件？

> 

### 7.2 修复

把 `status.o` 加入链接：

```bash

```

实际输出：

```text

```

这次链接器从哪个目标文件中找到了 `report_status` 的定义？

> 

---

## 练习 8：给错误分类

为每种情况填写最典型的失败阶段，并说明理由。

1. `#include "missing.hpp"`，系统找不到文件

   阶段：

   > 

   理由：

   > 

2. `int value{"Robot"};`

   阶段：

   > 

   理由：

   > 

3. 调用了已经声明的 `move()`，但所有目标文件和库都没有它的定义

   阶段：

   > 

   理由：

   > 

4. 程序启动时找不到所依赖的动态库

   阶段：

   > 

   理由：

   > 

5. 可执行程序能够启动，但运行到某处发生越界访问并崩溃

   阶段：

   > 

   理由：

   > 

---

## 练习 9：解释 `main` 前后发生了什么

判断下面的说法是否准确，并改写成完整表述：

> 操作系统打开可执行文件后，直接从 `main` 的第一条 C++ 语句开始执行。

**判断：**

> 

**完整表述：**

> 

`return 0;` 最终向谁报告了什么？

> 

在你的终端中运行 `pipeline` 后，查看退出状态并记录。

使用的命令：

```bash

```

退出状态：

```text

```

---

## 练习 10：独立完成一次完整构建实验

把 `pipeline.cpp` 修改为输出：

```text
Robot: Arm-B
Stage: linked
```

修改后的完整代码：

```cpp

```

要求：

1. 分别生成新的 `.ii`、`.s`、`.o` 和可执行文件
2. 不覆盖前一次实验的产物
3. 记录四条核心命令
4. 运行最终程序
5. 修改源代码后不要跳过重新构建

四条核心命令：

```bash

```

实际输出：

```text

```

如果只修改源文件但不重新构建，旧可执行文件为什么不会自动改变？

> 

---

## 学习复盘

**今天最清楚的知识点：**

> 

**仍然不理解或想继续追问的地方：**

> 

**你现在如何解释“编译成功但链接失败”：**

> 

完成后保存并同步到 GitHub，再在对话中请求批改第 4 天。
