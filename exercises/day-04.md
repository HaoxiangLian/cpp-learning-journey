# 第 4 天练习：条件、循环与作用域

## A. 笔试基础

### 1. 下面循环执行几次，依次输出什么

```cpp
for (int i{0}; i < 3; ++i) {
    std::cout << i << ' ';
}
```

> 
>

### 2. 把 `< 3` 改成 `<= 3` 后发生什么

> 
>

### 3. 预测 `count` 的最终值并说明短路过程

```cpp
int count{};
const bool ready{false};
const bool result{ready && (++count > 0)};
```

> 
>
>

## B. 编程题

编写五次循环。误差大于 `1.0` 输出 `ADJUST`，在 `[-1.0, 1.0]` 内输出 `OK`，小于 `-1.0` 输出 `REVERSE`。每次循环后把误差减 `1.5`。

```cpp

```

实际输出：

```text

```

## C. 调试题

构建 `examples/day-04/scope_error.cpp`，记录关键诊断。把输出语句移动到合法位置并重新构建。

```text

```

> 
>

## D. 面试口述

用 30～60 秒解释 `&&` 的短路求值，并给出“先检查指针或设备状态，再访问数据”的工程例子。

> 
>
>
>

