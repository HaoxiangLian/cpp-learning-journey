# 第 4 天练习单

主题：源码到可执行文件的完整链路

## 作答方式

- 文字题直接在题目下方的空白处填写
- 命令题在紧随题目的代码块中填写或修改
- 观察题先预测，再执行命令并记录实际结果
- 错误实验必须记录错误发生的阶段和关键诊断
- 完成后保存并同步到 GitHub

---

## 练习 1：还原完整链路

不要看讲义，按先后顺序填写缺失阶段：

```text
源文件
  ↓

  ↓
预处理后的翻译单元
  ↓

  ↓
汇编文本
  ↓

  ↓
可重定位目标文件
  ↓

  ↓
可执行文件
  ↓

  ↓
运行库启动代码
  ↓
main
```

**你的完整链路：**

```text

```

为什么“一条 `g++` 命令没有留下 `.ii` 和 `.s` 文件”不等于这些阶段没有发生？

> 

---

## 练习 2：区分参与者的职责

分别用一到两句话说明：

1. 编译器驱动程序：

   > 

2. 预处理器：

   > 

3. 狭义编译器：

   > 

4. 汇编器：

   > 

5. 链接器：

   > 

6. 操作系统装载器：

   > 

`g++` 在今天的命令中主要扮演哪一种角色？

> 

---

## 练习 3：判断命令停在哪里

填写每条命令的最后阶段、主要产物以及是否进行链接。

| 命令 | 最后阶段 | 主要产物 | 是否链接 |
|---|---|---|---|
| `g++ -E a.cpp -o a.ii` |  |  |  |
| `g++ -S a.cpp -o a.s` |  |  |  |
| `g++ -c a.cpp -o a.o` |  |  |  |
| `g++ a.cpp -o a` |  |  |  |
| `g++ a.o -o a` |  |  |  |

为什么对 `.cpp` 使用 `-c` 不等于“只执行汇编”？

> 

---

## 练习 4：看见狭义编译到底做了什么

使用 [`compile_stage.cpp`](../examples/day-04/compile_stage.cpp)：

```cpp
int main() {
    int target_position{10};
    int current_position{4};
    int error{target_position - current_position};
    int command{error * 2};
    return command;
}
```

### 4.1 先说清输入与输出

当执行 `g++ -S` 时，狭义编译阶段拿到的主要输入是什么？

> 

本实验最终保留下来的可见输出是什么？

> 

它为什么叫“汇编文本”，而不是目标文件？

> 

### 4.2 跟踪一行代码

针对：

```cpp
int error{target_position - current_position};
```

分别说明编译器要确认什么。不要照抄术语定义，要结合这一行中的具体名字和类型回答。

| 编译器工作 | 针对这一行要确认的具体内容 |
|---|---|
| 语法分析 |  |
| 名称查找 |  |
| 类型检查 |  |
| 翻译为更明确的计算 |  |

如果把 `target_position` 误写为 `target_postion`，最接近哪一项工作会发现问题？为什么？

> 

如果改成 `int error{"too far"};`，最接近哪一项工作会发现问题？为什么？

> 

### 4.3 比较 `-O0` 与 `-O2`

先建立 `build/day-04` 目录，然后写出两条命令，分别生成：

- `build/day-04/compile_stage-O0.s`
- `build/day-04/compile_stage-O2.s`

```bash

```

在两个文件中搜索 `main`。记录你看到的一个明显差异；不要求逐条翻译汇编。

> 

为什么优化版本可能直接准备返回值 `12`，而不保留源代码中的四个局部变量和两次算术步骤？

> 

### 4.4 用自己的话解释 IR

下面哪句话更准确？选择后说明理由。

A. IR 是所有 C++ 编译器都必须写到磁盘上的统一文件。  
B. IR 是编译器内部便于分析、优化和代码生成的一种或多种过渡表示。  
C. IR 就是处理器可以直接执行的机器指令。

选择：

> 

理由：

> 

“较低层表示”中的“较低层”相对于 C++ 源代码，具体少了什么、又把什么表达得更明确？

> 

---

## 练习 5：观察预处理结果

先阅读：

```cpp
#include <iostream>

#define ROBOT_NAME "Arm-A"

int main() {
    std::cout << "Robot: " << ROBOT_NAME << '\n';
    return 0;
}
```

### 4.1 预测

预处理后的文件与原文件相比，大小会怎样变化？为什么？

> 

预处理结果中，标识符 `ROBOT_NAME` 是否还会出现在使用位置？

> 

字符串 `Arm-A` 是否会出现在预处理结果中？

> 

### 4.2 执行

建立目录并写出生成 `pipeline.ii` 的命令：

```bash

```

### 4.3 实际观察

原始 `pipeline.cpp` 的大小：

> 

生成的 `pipeline.ii` 的大小：

> 

搜索 `ROBOT_NAME` 后看到的结果：

```text

```

搜索 `Arm-A` 后看到的关键位置：

```text

```

预测与实际是否一致？如果不一致，差异是什么？

> 

---

## 练习 6：生成并辨认四种产物

依次写出命令，生成：

1. `build/day-04/pipeline.ii`
2. `build/day-04/pipeline.s`
3. `build/day-04/pipeline.o`
4. `build/day-04/pipeline` 或 `pipeline.exe`

```bash

```

完成后填写：

| 文件 | 文本还是二进制 | 能否直接作为正常程序运行 | 你观察到的一个特征 |
|---|---|---|---|
| `pipeline.cpp` |  |  |  |
| `pipeline.ii` |  |  |  |
| `pipeline.s` |  |  |  |
| `pipeline.o` |  |  |  |
| `pipeline` / `pipeline.exe` |  |  |  |

运行最终程序后的实际输出：

```text

```

尝试直接运行 `pipeline.o` 后发生了什么？

```text

```

为什么目标文件通常不能直接运行？

> 

---

## 练习 7：用“供给、需求、待修正位置”解释符号和重定位

分别生成：

```bash
g++ -std=c++17 -Wall -Wextra -pedantic -c examples/day-04/link_main.cpp -o build/day-04/link_main.o
g++ -std=c++17 -Wall -Wextra -pedantic -c examples/day-04/status.cpp -o build/day-04/status.o
```

在运行工具前先预测：

| 目标文件 | `report_status()` 是已定义还是未定义 | 为什么 |
|---|---|---|
| `link_main.o` |  |  |
| `status.o` |  |  |

如果当前工具链提供 `nm`，执行：

```bash
nm -C build/day-04/link_main.o
nm -C build/day-04/status.o
```

记录与预测相符的关键行：

```text

```

`U report_status()` 中的 `U` 在表达什么需求？

> 

`T report_status()` 中的 `T` 在表达什么供给？

> 

如果当前工具链提供 `objdump`，执行：

```bash
objdump -r build/day-04/link_main.o
```

记录一条与函数调用有关的重定位项；如果平台输出不同，则记录你实际看到的内容。

```text

```

补全下面两句话：

> 符号记录的是：

> 重定位记录的是：

为什么编译 `link_main.cpp` 时，编译器不能直接把 `report_status` 的最终地址写进调用指令？

> 

如果系统没有 `nm` 或 `objdump`，记录工具提示：

```text

```

---

## 练习 8：制造并修复链接错误

先分别生成两个目标文件：

```bash

```

### 7.1 故意失败

只使用 `link_main.o` 进行链接：

```bash

```

关键错误信息：

```text

```

这个错误属于哪个阶段？

> 

为什么 `link_main.cpp` 能生成目标文件，最后却不能生成可执行文件？

> 

### 7.2 修复

把 `status.o` 加入链接：

```bash

```

实际输出：

```text

```

这次链接器从哪个目标文件中找到了 `report_status` 的定义？

> 

---

## 练习 9：给错误分类

为每种情况填写最典型的失败阶段，并说明理由。

1. `#include "missing.hpp"`，系统找不到文件

   阶段：

   > 

   理由：

   > 

2. `int value{"Robot"};`

   阶段：

   > 

   理由：

   > 

3. 调用了已经声明的 `move()`，但所有目标文件和库都没有它的定义

   阶段：

   > 

   理由：

   > 

4. 程序启动时找不到所依赖的动态库

   阶段：

   > 

   理由：

   > 

5. 可执行程序能够启动，但运行到某处发生越界访问并崩溃

   阶段：

   > 

   理由：

   > 

---

## 练习 10：解释 `main` 前后发生了什么

判断下面的说法是否准确，并改写成完整表述：

> 操作系统打开可执行文件后，直接从 `main` 的第一条 C++ 语句开始执行。

**判断：**

> 

**完整表述：**

> 

`return 0;` 最终向谁报告了什么？

> 

在你的终端中运行 `pipeline` 后，查看退出状态并记录。

使用的命令：

```bash

```

退出状态：

```text

```

---

## 练习 11：独立完成一次完整构建实验

把 `pipeline.cpp` 修改为输出：

```text
Robot: Arm-B
Stage: linked
```

修改后的完整代码：

```cpp

```

要求：

1. 分别生成新的 `.ii`、`.s`、`.o` 和可执行文件
2. 不覆盖前一次实验的产物
3. 记录四条核心命令
4. 运行最终程序
5. 修改源代码后不要跳过重新构建

四条核心命令：

```bash

```

实际输出：

```text

```

如果只修改源文件但不重新构建，旧可执行文件为什么不会自动改变？

> 

---

## 练习 12：求职面试口述

不看讲义，先用 30～60 秒回答：

> 一份 C++ 源代码通常怎样变成可执行文件？狭义编译阶段具体做什么？

**第一次口述提纲：**

> 

回答必须至少包含：

- `g++` 驱动程序与狭义编译器的区别
- 预处理后的输入
- 语法、名称、类型和语义检查
- IR、优化与汇编输出各自处于什么位置
- `-S` 与 `-c` 的边界
- 为什么编译成功后仍可能链接失败

然后回答追问：

> “编译器把代码变成更低层表示”中的“更低层”究竟是什么意思？

**你的回答：**

> 

> 为什么 `report_status();` 只有声明也能编译，却可能链接失败？

**你的回答：**

> 

口述后，删掉任何你只能背诵、不能结合 `compile_stage.cpp` 或两个 `.o` 文件解释的句子，再重新组织一次。

**第二次口述提纲：**

> 

---

## 学习复盘

**今天最清楚的知识点：**

> 

**仍然不理解或想继续追问的地方：**

> 

**你现在如何解释“编译成功但链接失败”：**

> 

完成后保存并同步到 GitHub，再在对话中请求批改第 4 天。
