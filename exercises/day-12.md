# 第 12 天练习：动态存储、new、delete 与所有权

姓名：  
日期：

## 练习 1：术语辨析

用自己的话区分“指针”“所有者”“观察者”和“别名”。说明为什么一个原始指针的类型本身不能证明它是否负责 `delete`。

>
>
>
>

## 练习 2：跟踪对象与存储

逐行说明下面程序中动态对象的生命周期、动态存储何时取得和释放，以及 `pointer` 自身的存储期。

```cpp
int* pointer{new int{7}};
*pointer = 9;
delete pointer;
pointer = nullptr;
```

>
>
>
>
>

## 练习 3：预测并验证安全示例

先预测 `dynamic_storage_demo.cpp` 的三行输出，再编译运行并记录实际结果。

### 预测

>
>
>

### 实际结果

>
>
>

### 差异解释

>
>
>

## 练习 4：修复悬空别名

在不使用智能指针的前提下重写下面代码，使 `observer` 不会在对象销毁后被解引用。随后解释：只执行 `owner = nullptr` 为什么不能修复 `observer`。

```cpp
int* owner{new int{42}};
int* observer{owner};
delete owner;
owner = nullptr;
return *observer;
```

```cpp

```

>
>
>

## 练习 5：匹配数组形式

编写程序，使用 `new[]` 创建 5 个 `double`，列表初始化为 `1.0` 到 `5.0`，求和后使用正确形式释放。释放后把持有地址的指针改为 `nullptr`。

```cpp

```

## 练习 6：错误分类

分别说明下列问题违反了什么规则、属于哪个阶段的问题，以及 Sanitizer 可能报告什么：

1. `new[]` 后使用 `delete`
2. 对同一分配结果执行两次 `delete`
3. `delete` 后通过别名读取对象
4. 丢失最后一个指向动态分配存储的指针

>
>
>
>
>
>

## 练习 7：预测 `delete nullptr`

```cpp
int* pointer{nullptr};
delete pointer;
```

### 预测

>
>

### 实际结果

>
>

### 规则解释

>
>

## 练习 8：所有权设计

一个函数创建动态数组并返回原始指针，调用者必须另行知道长度和释放方式。列出至少三个接口风险，并分别说明第 20、23、28 天的哪些机制会改善它。

>
>
>
>
>

## 练习 9：完整编程题

编写 `make_measurement` 函数动态创建一个 `int`，编写只观察而不释放的 `print_measurement`，再由 `main` 中唯一所有者完成释放。要求：

- 准确区分形参与实参
- 不泄漏、不重复释放、不在释放后访问
- 释放后将仍由 `main` 持有的指针置空
- 在答案下说明每个指针的所有权角色

```cpp

```

>
>
>

## 自检

- [ ] 我能解释 `new` 表达式不只是“返回一块内存”
- [ ] 我能说明 `delete` 的销毁与释放两个作用
- [ ] 我能正确匹配 `new`/`delete` 和 `new[]`/`delete[]`
- [ ] 我能追踪所有者、观察者与别名
- [ ] 我能识别泄漏、释放后使用和重复释放
- [ ] 我使用 Sanitizer 复现并解释了错误实验
