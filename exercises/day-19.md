# 第 19 天练习：构造函数、成员初始化列表与 `this`

## 练习 1：对象构造全过程

以 `Joint shoulder{"shoulder", -90.0, 120.0};` 为例，按顺序说明存储获得、成员初始化、构造函数体执行和对象生命周期开始之间的关系。不要把构造函数体说成初始化成员的唯一位置。

>
>
>
>
>

## 练习 2：初始化与赋值

解释下面两种写法的区别。指出第二种写法中 `name_` 在进入构造函数体之前发生了什么。

```cpp
class A {
public:
    A(const std::string& name) : name_{name} {}
private:
    std::string name_;
};
```

```cpp
class B {
public:
    B(const std::string& name) { name_ = name; }
private:
    std::string name_;
};
```

>
>
>
>

## 练习 3：预测成员初始化顺序

阅读 `examples/day-19/initialization_order.cpp`。不要运行，先预测输出顺序，并解释初始化列表书写顺序是否能改变成员初始化顺序；再编译运行并记录实际结果和警告。

### 预测

>
>
>

### 实际结果

>
>
>
>

## 练习 4：修正初始化顺序

修改 `Robot` 的构造函数初始化列表，使书写顺序与成员声明顺序一致，并写出修改后的类定义。

```cpp

```

## 练习 5：默认构造函数

解释 `default_constructor_error.cpp` 为什么无法编译。为 `Sensor` 设计一种合理修复，并说明你选择“提供默认状态”还是“要求调用者必须给出通道”的工程理由。

>
>
>
>

```cpp

```

## 练习 6：`explicit`

解释 `explicit Joint(const std::string& name);` 防止了哪类隐式转换。写出直接初始化一个 `Joint` 的正确代码，并说明为什么 `Joint joint = std::string{"wrist"};` 不被该构造函数允许。

>
>
>
>

```cpp

```

## 练习 7：`this` 与链式调用

解释 `this->position_ = position;` 中 `this` 的类型与 `*this` 的含义。说明 `return *this;` 为什么能支持连续两次 `set_position`。

>
>
>
>

## 练习 8：`const` 成员函数

为下面类补写一个可在 `const Motor&` 上调用的 `speed()`，再解释为什么该函数不能直接修改 `speed_`。

```cpp

```

>
>
>

## 练习 9：类不变量

为 `Joint` 写出至少两个应在公开操作后保持成立的类不变量。判断当前示例中的 `assert` 是否适合检查外部不可信输入，并说明第 32 天还需要补充什么。

>
>
>
>
>

## 练习 10：跨翻译单元静态初始化

解释为什么一个命名空间作用域对象的构造函数不应依赖另一个 `.cpp` 中尚未保证完成动态初始化的对象。写出“函数局部静态对象＋访问函数”的安全结构。

>
>
>
>

```cpp

```

## 完成核对

- [ ] 已区分对象初始化与构造函数体内赋值
- [ ] 已验证成员按声明顺序初始化
- [ ] 已解释默认构造函数何时不存在
- [ ] 已解释 `explicit`、委托构造和默认成员初始化器
- [ ] 已解释 `this`、`*this` 与 `const` 成员函数
- [ ] 已写出并维护简单类不变量
- [ ] 已识别跨翻译单元静态初始化顺序风险
