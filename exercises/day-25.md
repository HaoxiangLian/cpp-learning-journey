# 第 25 天｜求职版练习

> 配套讲义：[运算符重载、流表达式与接口设计](../days/第25天-运算符重载、流表达式与接口设计.md)

## 作答规则

- 先预测，再编译或运行；实际与预测不同就记录第一处差异。
- 所有 C++ 代码使用 C++17，并启用 `-Wall -Wextra -pedantic`；线程题额外使用 `-pthread`。
- 故意失败的程序要写清失败阶段、关键诊断、根因和验证证据。
- 每道题的答案直接写在题目下方。
- 四类任务全部达到验收标准，本单元才算完成。

---

## A｜基础概念检查

1. 为什么 `operator<<` 通常返回 `std::ostream&`？

   > 

2. `operator+` 应写成员还是非成员，判断依据是什么？

   > 

3. 这个方案有什么限制，什么时候不该使用？

   > 

4. 用本课最小示例说明：哪个对象、文件或调用在什么语句前后发生了变化？

   > 

---

## B｜可运行编程任务

### 任务

为 `Vector2` 实现 `operator+=`、基于它的非成员 `operator+`，以及 `operator<<`；证明 `a + b` 不修改 `a`。

### 起始代码

```cpp
#include <iostream>

struct Vector2 {
    double x{};
    double y{};

    Vector2& operator+=(const Vector2& other) {
        // TODO
        return *this;
    }
};

// TODO: operator+
// TODO: operator<<

int main() {
    Vector2 a{1.0, 2.0};
    Vector2 b{3.0, -1.0};
    Vector2 c = a + b;
    std::cout << "a = " << a << "\n";
    std::cout << "c = " << c << "\n";
}
```

### 预期结果

```text
a = (1, 2)
c = (4, 1)
```

### 验收标准

- [ ] `operator+=` 返回 `*this` 的引用
- [ ] `operator+` 按值接收左操作数并复用 `+=`
- [ ] `operator<<` 返回传入的 `ostream&`

### 你的实现

```cpp

```

使用的构建命令：

```bash

```

运行前的输出预测：

> 

实际输出：

```text

```

预测与实际的第一处差异：

> 

---

## C｜调试任务

### 故障代码

```cpp
struct Counter { int value{}; };

Counter operator+(const Counter& left, const Counter& right) {
    std::cout << "sending command\n"; // 隐藏副作用
    return {left.value - right.value}; // 还违背加法直觉
}
```

### 调试要求

先不要修改。判断问题首先出现在编译、链接、运行、结果还是接口设计；运行或构建后，只保留最关键的诊断。修复时说明为什么最小修改能针对根因，而不是只让报错消失。

运行或构建前的预测：

> 

问题首先出现的阶段：

> 

关键诊断或异常现象：

```text

```

根本原因：

> 

修复后的代码：

```cpp

```

为什么这个修复有效：

> 

修复后的验证输入与结果：

```text

```

---

## D｜求职面试口述

先用 30～60 秒回答本课核心问题。回答必须包含：

- 能把运算符表达式改写成相应函数调用理解
- 能解释流链式调用
- 不会用运算符隐藏意外副作用

参考结构是“具体问题 → 对象或文件怎样变化 → 使用的规则 → 一个限制”，不要堆术语。

第一次口述提纲：

> 

面试官追问：

> 为什么 `operator<<` 通常返回 `std::ostream&`？

你的回答：

> 

再追问：

> `operator+` 应写成员还是非成员，判断依据是什么？

你的回答：

> 

删掉所有不能回到代码、输出、诊断或测试证据的句子，再写第二版提纲：

> 

---

## 学习复盘

今天最清楚的机制：

> 

仍然不能通过代码解释的地方：

> 

我能否在不看讲义时复现程序和故障：

> 

本单元是否达到四类任务的验收标准：

> 
