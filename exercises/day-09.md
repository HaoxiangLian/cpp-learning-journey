# 第 9 天练习单

主题：机器人关节状态程序综合训练  
语言标准：C++17

## 作答方式

- 直接在每道题下方的空白区填写答案。
- 预测题先写预测，再编译运行并记录实际结果。
- 代码题在紧随题目的空 `cpp` 代码块中完成。
- 错误实验保留关键诊断，并判断错误发生阶段。

---

## 练习 1：从需求推导程序组成

需求如下：

> 保存多个机器人关节；每个关节有编号、名称、角度和使能状态；可以按编号更新角度；只接受 `[-180, 180]`；更新失败时不得改变原状态；能够输出完整报告。

### 1.1 哪些信息适合组成 `JointState`？

> 

### 1.2 哪些状态适合由 `RobotMonitor` 统一维护？

> 

### 1.3 更新函数至少需要哪些参数和返回值？

> 

### 1.4 角度范围属于 C++ 语言规则还是本项目业务规则？

> 

---

## 练习 2：设计数据模型

完成以下要求：

1. `using JointId = int`
2. `enum class Mode`，包含 `offline`、`ready`、`running`、`fault`
3. `struct JointState`，包含编号、名称、角度和使能状态
4. 创建一个编号为 2、名称为 `shoulder`、角度为 35、已使能的对象

```cpp

```

### 为什么 `Mode` 不直接使用任意整数？

> 

### `JointId` 是否是区别于 `int` 的新类型？

> 

---

## 练习 3：函数职责与接口

分别写出以下函数的单一职责：

### 3.1 `std::string mode_name(Mode mode)`

> 

### 3.2 `bool update_angle(JointId id, double angle_deg)`

> 

### 3.3 `int enabled_count() const`

> 

### 3.4 `void print_report() const`

> 

### 为什么 `update_angle` 不应只返回 `void`？

> 

---

## 练习 4：预测角度校验

```cpp
bool valid_angle(double angle_deg) {
    if (angle_deg < -180.0 || angle_deg > 180.0) {
        return false;
    }
    return true;
}
```

分别预测：

| 输入 | 预测返回值 | 实际返回值 |
|---:|---|---|
| -180.1 |  |  |
| -180.0 |  |  |
| 0.0 |  |  |
| 180.0 |  |  |
| 180.1 |  |  |

### 为什么 `-180` 和 `180` 被接受？

> 

---

## 练习 5：跟踪线性查找

初始状态：

```cpp
std::vector<JointState> joints{
    {1, "base", 0.0, true},
    {2, "shoulder", 35.0, true},
    {3, "tool", -10.0, false}
};
```

调用：

```cpp
update_angle(2, 47.5);
```

### 5.1 按顺序比较了哪些编号？

> 

### 5.2 哪个对象被修改？

> 

### 5.3 找到目标后是否继续检查关节 3？为什么？

> 

### 5.4 若编号为 9，会检查多少个元素并返回什么？

> 

---

## 练习 6：实现正确的更新函数

编写独立函数：

```cpp
bool update_angle(std::vector<JointState>& joints,
                  JointId id,
                  double angle_deg);
```

要求：

- 角度越界返回 `false`
- 编号不存在返回 `false`
- 成功时修改真实元素并返回 `true`
- 失败时原序列不变

```cpp

```

### 解释每个参数为什么采用当前传递方式

> 

---

## 练习 7：预测副本修改错误

阅读：

```cpp
for (JointState joint : joints) {
    if (joint.id == id) {
        joint.angle_deg = angle_deg;
        return true;
    }
}
```

### 7.1 预测：函数是否可能返回 `true`？

> 

### 7.2 预测：向量中的原角度是否改变？

> 

### 7.3 实际结果

> 

### 7.4 这是语法错误、链接错误、运行期崩溃还是逻辑错误？

> 

### 7.5 写出修复后的循环首行

```cpp

```

---

## 练习 8：只读遍历与 `const` 成员函数

完成统计函数：

```cpp
int enabled_count() const {
    
}
```

要求使用范围 `for` 和 `const JointState&`。

```cpp

```

### 为什么这里只需要只读引用？

> 

### 函数末尾的 `const` 修饰什么？

> 

---

## 练习 9：实现使能状态更新

为 `RobotMonitor` 增加：

```cpp
bool set_enabled(JointId id, bool enabled);
```

要求：

- 找到编号后修改真实关节并返回 `true`
- 未找到时返回 `false`
- 不输出错误信息，由调用方根据返回值决定怎样处理

```cpp

```

### 为什么让函数返回结果比在函数内部固定输出更容易复用？

> 

---

## 练习 10：错误实验——私有成员

编译：

```bash
g++ -std=c++17 -Wall -Wextra -Wpedantic \
    examples/day-09/private_access_error.cpp \
    -o private_access_error
```

### 10.1 是否生成可执行文件？

> 

### 10.2 关键诊断

> 

### 10.3 错误发生在预处理、狭义编译、汇编、链接还是运行阶段？

> 

### 10.4 应通过哪个公开接口修改模式？

```cpp

```

---

## 练习 11：设计测试用例

为 `update_angle` 补全测试表：

| 测试目标 | 输入或初始状态 | 预期返回值 | 预期状态变化 |
|---|---|---|---|
| 正常更新 |  |  |  |
| 编号不存在 |  |  |  |
| 等于下界 |  |  |  |
| 小于下界 |  |  |  |
| 等于上界 |  |  |  |
| 大于上界 |  |  |  |
| 空序列 |  |  |  |

### 哪些属于边界情况？

> 

---

## 练习 12：完整编程

不复制主示例，独立完成一个机器人关节状态监控程序：

1. 类型放入 `robotics` 命名空间
2. 使用 `JointId`、`Mode`、`JointState` 和 `RobotMonitor`
3. 至少保存三个关节
4. 提供设置名称、设置模式、添加关节、更新角度、更新使能状态、统计使能数量和输出报告的接口
5. 数据成员保持私有
6. 至少测试一次成功更新、一次编号不存在、一次角度越界和两个合法端点
7. 失败操作不能改变原状态
8. 通过严格警告和 ASan/UBSan 验证

```cpp

```

### 测试数据与预测

> 

### 实际输出

> 

### 编译警告

> 

### Sanitizer 结果

> 

---

## 练习 13：解释完整构建链路

执行：

```bash
g++ -std=c++17 -Wall -Wextra -Wpedantic \
    examples/day-09/robot_joint_monitor.cpp \
    -o robot_joint_monitor
```

### 13.1 按顺序写出这条命令背后的完整构建阶段

> 

### 13.2 哪个阶段主要检查 C++ 语法、名称和类型规则？

> 

### 13.3 哪个阶段解析跨目标文件和库的符号引用？

> 

### 13.4 可执行文件启动后，操作系统通常是否直接跳到 `main`？

> 

---

## 练习 14：错误分类

分别判断：

| 现象 | 错误或诊断阶段 | 理由 |
|---|---|---|
| 访问私有成员 |  |  |
| 忘记链接函数定义 |  |  |
| 按值遍历导致原对象未改变 |  |  |
| `vector` 越界被 ASan 报告 |  |  |
| 找不到头文件 |  |  |

---

## 学习复盘

### 1. 从需求到代码，依次做了哪些决策？

> 

### 2. `JointState` 与 `RobotMonitor` 的职责有什么不同？

> 

### 3. 接口为什么不仅是函数名？

> 

### 4. 为什么要区分业务规则和 C++ 语言规则？

> 

### 5. 为什么编译成功不能证明程序正确？

> 

### 6. 为什么测试必须覆盖失败路径和边界情况？

> 

### 7. 本单元仍然暂缓到后续的内容有哪些？

> 

### 8. 本单元最容易混淆的内容

> 
