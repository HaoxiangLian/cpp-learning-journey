# 第 9 天：机器人关节状态程序综合训练｜求职版练习

## 作答规则

- 先预测，再编译或运行；实际结果与预测不同时，必须记录差异。
- 所有代码使用 C++17，并开启 `-Wall -Wextra -pedantic`。
- 故意失败的程序必须写明失败阶段、关键诊断和修复依据。
- 每道题的答案直接写在题目下方。
- 四类必做任务全部通过后，本单元才算完成。

---

## A｜基础概念检查

1. 如何把自然语言需求转换成函数接口和测试用例？

   > 

2. 为什么范围循环中的 `auto state` 可能导致修改无效？

   > 

3. 结合本课最小代码，按顺序说明其中至少三个对象、值或文件发生了什么变化。

   > 

---

## B｜可运行编程任务

### 任务

完成一个最小关节监视器：按编号更新角度，非法编号或超范围角度返回 `false` 且不修改原状态。

### 起始代码

```cpp
#include <iostream>
#include <vector>

struct JointState { int id{}; double angle{}; };

bool update_angle(std::vector<JointState>& joints, int id, double angle) {
    // 校验、查找、修改
}

int main() {
    std::vector<JointState> joints{{1, 0.0}, {2, 10.0}};
    // 至少运行四个测试
}
```

### 预期结果

```text
正常更新成功；非法编号、181 度和 -181 度均失败；失败后原状态不变
```

### 验收标准

- [ ] 先写出至少四个测试再完成实现
- [ ] 修改真实元素而不是副本
- [ ] 所有路径有明确返回值
- [ ] 严格警告和 Sanitizer 构建通过

### 你的实现

```cpp

```

使用的构建命令：

```bash

```

实际输出：

```text

```

预测与实际的差异：

> 

---

## C｜调试任务

### 故障代码

```cpp
for (auto joint : joints) {
    if (joint.id == id) {
        joint.angle = angle;
        return true;
    }
}
```

### 调试要求

解释为什么函数返回 `true` 却没有改变 `vector` 中的元素。用对象副本与元素引用的关系修复。

运行或构建前的预测：

> 

错误发生的阶段：

> 

关键诊断或异常现象：

```text

```

根本原因：

> 

修复后的代码：

```cpp

```

为什么这个修复有效：

> 

---

## D｜求职面试口述

先用 30～60 秒回答：

> 如何把自然语言需求转换成函数接口和测试用例？

答案必须能够回到本课实验中的具体事实，例如：

- 需求：编号存在且角度在安全范围时更新，否则保持原状态
- 数据模型：每个 `JointState` 保存编号、角度和模式
- 查找：遍历关节序列，比较 `id`
- 修改：必须取得目标元素的引用，不能只修改副本

第一次口述提纲：

> 

面试追问：

> 为什么范围循环中的 `auto state` 可能导致修改无效？

你的回答：

> 

删掉任何只能背诵、不能由代码或诊断推导出的句子，再写第二版提纲：

> 

---

## 学习复盘

今天最清楚的机制：

> 

仍然不能通过代码解释的地方：

> 

本单元是否达到四类任务的验收标准：

> 
