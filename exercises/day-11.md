# 第 11 天练习单

主题：对象存储期、生命周期与栈堆模型  
语言标准：C++17

## 作答方式

- 直接在每道题下方的空白区填写答案。
- 预测题先写预测，再编译运行并记录实际结果。
- 代码题在紧随题目的空 `cpp` 代码块中完成。
- 错误实验保留关键诊断，并判断语言根因与工具表现。

---

## 练习 1：区分六个维度

阅读：

```cpp
int next_value() {
    static int counter{};
    ++counter;
    return counter;
}
```

### 1.1 `counter` 的名称作用域在哪里？

> 

### 1.2 `counter` 对象是什么类型？

> 

### 1.3 它具有什么存储期？

> 

### 1.4 它的生命周期是否在函数返回时结束？

> 

### 1.5 “名称在函数外不可见”和“对象已经不存在”是否等价？

> 

### 1.6 链接属性描述的是什么问题？

> 

---

## 练习 2：判断存储期

```cpp
int global_value{};
thread_local int thread_value{};

void function(int parameter) {
    int automatic_value{};
    static int static_value{};
}
```

| 名称 | 存储期 | 判断理由 |
|---|---|---|
| `global_value` |  |  |
| `thread_value` |  |  |
| `parameter` |  |  |
| `automatic_value` |  |  |
| `static_value` |  |  |

### 哪两个名称都在块作用域，但存储期不同？

> 

---

## 练习 3：预测自动局部对象

```cpp
#include <iostream>

void count() {
    int value{};
    ++value;
    std::cout << value << '\n';
}

int main() {
    count();
    count();
    count();
}
```

### 3.1 预测

> 

### 3.2 实际结果

> 

### 3.3 三次调用使用的是同一个 `value` 对象吗？

> 

---

## 练习 4：预测局部静态对象

把练习 3 中的定义改成：

```cpp
static int value{};
```

### 4.1 预测

> 

### 4.2 实际结果

> 

### 4.3 名称作用域是否因 `static` 变成全局？

> 

### 4.4 哪个维度发生了关键变化？

> 

---

## 练习 5：跟踪递归对象

```cpp
void trace(int depth) {
    int marker{depth};
    std::cout << "enter " << marker << '\n';
    if (depth > 0) {
        trace(depth - 1);
    }
    std::cout << "leave " << marker << '\n';
}
```

调用 `trace(3)`。

### 5.1 预测完整输出顺序

> 

### 5.2 实际结果

> 

### 5.3 最深处执行时，同时存在多少个 `depth` 参数对象和 `marker` 对象？

> 

### 5.4 为什么内层调用没有覆盖外层 `marker`？

> 

---

## 练习 6：语言规则与常见实现

判断下列说法是否准确，并改写错误说法。

### 6.1 所有局部变量都在栈上

> 

### 6.2 所有动态存储期对象都必须位于操作系统定义的同一个堆

> 

### 6.3 自动存储期是 C++ 语言概念，栈帧是常见 ABI/实现概念

> 

### 6.4 编译器可以把不可观察的自动对象放入寄存器或优化掉

> 

---

## 练习 7：初始化与赋值

```cpp
int value{10};
value = 20;
```

### 7.1 哪一行是初始化？

> 

### 7.2 哪一行是赋值？

> 

### 7.3 两者发生时，对象生命周期状态有什么不同？

> 

---

## 练习 8：未初始化读取警告实验

编译但不要运行：

```bash
g++ -std=c++17 -O2 -Wall -Wextra -Wpedantic \
    examples/day-11/uninitialized_read.cpp \
    -o uninitialized_read
```

### 8.1 当前编译器是否产生警告？

> 

### 8.2 关键诊断

> 

### 8.3 若换一个编译器没有警告，读取是否因此合法？

> 

### 8.4 为什么不能把读取结果称为“随机数”？

> 

---

## 练习 9：分析悬空指针

```cpp
int* pointer{nullptr};

{
    int local{42};
    pointer = &local;
}
```

### 9.1 离开块后 `pointer` 是否自动变成 `nullptr`？

> 

### 9.2 `local` 的名称作用域和对象生命周期分别发生了什么？

> 

### 9.3 为什么 `pointer != nullptr` 仍不能证明可以解引用？

> 

### 9.4 空指针与悬空指针的区别是什么？

> 

---

## 练习 10：ASan 错误实验

```bash
g++ -std=c++17 -O1 -g -Wall -Wextra -Wpedantic \
    -fsanitize=address -fsanitize-address-use-after-scope \
    -fno-omit-frame-pointer \
    examples/day-11/use_after_scope.cpp \
    -o use_after_scope_san

./use_after_scope_san
```

### 10.1 程序退出状态是否为 0？

> 

### 10.2 关键诊断

> 

### 10.3 报告中的 `stack-use-after-scope` 描述哪个实现现象？

> 

### 10.4 C++ 语言层面的根因是什么？

> 

---

## 练习 11：命名空间对象与链接属性边界

```cpp
int namespace_value{};
const int limit{10};
static int internal_value{};
```

### 11.1 三个对象具有什么存储期？

> 

### 11.2 能否仅凭静态存储期断言三个名称都有外部链接？

> 

### 11.3 哪个后续单元负责完整解释链接属性？

> 

---

## 练习 12：动态存储先看边界

```cpp
int* pointer{new int{42}};
delete pointer;
pointer = nullptr;
```

### 12.1 `pointer` 指向的对象具有什么存储期？

> 

### 12.2 `delete` 后为什么要把当前不再使用的指针改为 `nullptr`？

> 

### 12.3 仅设为 `nullptr` 是否能修复其他仍保存旧地址的指针？

> 

### 12.4 完整分配、释放和所有权由哪一天学习？

> 

---

## 练习 13：修改主示例

为主示例增加：

1. 一个新的命名空间作用域计数器
2. 一个返回局部静态计数值的函数
3. 一个有两层递归的跟踪函数
4. 每处输出前写出预测
5. 严格警告构建并运行

```cpp

```

### 预测

> 

### 实际结果

> 

### 编译警告

> 

---

## 练习 14：分类综合题

| 现象 | 主要涉及的概念 | 典型发现阶段 |
|---|---|---|
| 块外使用普通局部名称 |  |  |
| 未初始化 `int` 被警告 |  |  |
| 局部 `static` 跨调用保留值 |  |  |
| 悬空指针被 ASan 报告 |  |  |
| 递归过深耗尽运行资源 |  |  |
| 两个翻译单元重复定义 |  |  |

---

## 练习 15：完整解释题

用不超过 300 字解释下面这句话为什么不准确：

> 局部变量放在栈上，离开大括号就被删除；全局变量一直放在全局区；`new` 出来的变量放在堆上。

回答必须使用：

- 名称
- 对象
- 作用域
- 自动/静态/动态存储期
- 生命周期
- 调用栈或堆属于常见实现

> 

---

## 练习 16：完整构建与运行模型

### 16.1 写出主示例从源码到进入 `main` 的完整链路

> 

### 16.2 未初始化读取警告通常在哪一阶段出现？

> 

### 16.3 ASan 的悬空访问诊断发生在构建期还是运行期？

> 

### 16.4 为什么链接成功不能证明所有对象生命周期访问合法？

> 

---

## 学习复盘

### 1. 作用域、存储期、生命周期和链接属性分别回答什么问题？

> 

### 2. 四类存储期分别是什么？

> 

### 3. 为什么局部 `static` 是反驳“局部等于短生命周期”的好例子？

> 

### 4. 为什么自动存储期对象不一定物理位于栈上？

> 

### 5. 为什么指针非空不能证明对象仍然存活？

> 

### 6. 编译警告、Sanitizer 和语言规则之间是什么关系？

> 

### 7. 本单元仍暂缓到后续的内容有哪些？

> 

### 8. 本单元最容易混淆的内容

> 
