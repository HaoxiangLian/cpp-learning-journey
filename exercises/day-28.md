# 第 28 天｜求职版练习

> 配套讲义：[顺序容器、迭代器与失效规则](../days/第28天-顺序容器、迭代器与失效规则.md)

## 作答规则

- 先预测，再编译或运行；实际与预测不同就记录第一处差异。
- 所有 C++ 代码使用 C++17，并启用 `-Wall -Wextra -pedantic`；线程题额外使用 `-pthread`。
- 故意失败的程序要写清失败阶段、关键诊断、根因和验证证据。
- 每道题的答案直接写在题目下方。
- 四类任务全部达到验收标准，本单元才算完成。

---

## A｜基础概念检查

1. `vector` 的 `size` 与 `capacity` 有什么区别？

   > 

2. 为什么链表插入是常数复杂度仍不一定比 `vector` 快？

   > 

3. 这个方案有什么限制，什么时候不该使用？

   > 

4. 用本课最小示例说明：哪个对象、文件或调用在什么语句前后发生了变化？

   > 

---

## B｜可运行编程任务

### 任务

向 `vector` 加入 1～8，记录每次 `size`、`capacity` 和 `data` 是否变化；只保存索引，不在扩容后使用旧指针。

### 起始代码

```cpp
#include <iostream>
#include <vector>

int main() {
    std::vector<int> values;
    const int* previous_data = values.data();

    for (int value = 1; value <= 8; ++value) {
        values.push_back(value);
        const bool moved = values.data() != previous_data;
        // TODO: 输出 value、size、capacity、moved
        previous_data = values.data();
    }

    const std::size_t saved_index = 2;
    // TODO: 继续 push_back 后通过 saved_index 重新访问
}
```

### 预期结果

```text
输出 8 行记录；至少能指出发生扩容的行；最终通过索引读到第 3 个元素为 3。
```

### 验收标准

- [ ] 访问合法性按 `size` 判断，不按 `capacity`
- [ ] 没有解引用可能失效的旧指针或迭代器
- [ ] 能用 `data()` 变化证明至少一次重分配

### 你的实现

```cpp

```

使用的构建命令：

```bash

```

运行前的输出预测：

> 

实际输出：

```text

```

预测与实际的第一处差异：

> 

---

## C｜调试任务

### 故障代码

```cpp
std::vector<int> values{1, 2, 3};
int& first = values[0];
values.push_back(4); // 可能重分配
std::cout << first;  // 可能使用失效引用
```

### 调试要求

先不要修改。判断问题首先出现在编译、链接、运行、结果还是接口设计；运行或构建后，只保留最关键的诊断。修复时说明为什么最小修改能针对根因，而不是只让报错消失。

运行或构建前的预测：

> 

问题首先出现的阶段：

> 

关键诊断或异常现象：

```text

```

根本原因：

> 

修复后的代码：

```cpp

```

为什么这个修复有效：

> 

修复后的验证输入与结果：

```text

```

---

## D｜求职面试口述

先用 30～60 秒回答本课核心问题。回答必须包含：

- 记录一次容量变化而不使用失效位置
- 能为三种访问模式选择容器
- 能解释至少两条具体失效规则

参考结构是“具体问题 → 对象或文件怎样变化 → 使用的规则 → 一个限制”，不要堆术语。

第一次口述提纲：

> 

面试官追问：

> `vector` 的 `size` 与 `capacity` 有什么区别？

你的回答：

> 

再追问：

> 为什么链表插入是常数复杂度仍不一定比 `vector` 快？

你的回答：

> 

删掉所有不能回到代码、输出、诊断或测试证据的句子，再写第二版提纲：

> 

---

## 学习复盘

今天最清楚的机制：

> 

仍然不能通过代码解释的地方：

> 

我能否在不看讲义时复现程序和故障：

> 

本单元是否达到四类任务的验收标准：

> 
