# 第 32 天｜求职版练习

> 配套讲义：[错误处理、异常与 optional](../days/第32天-错误处理、异常与optional.md)

## 作答规则

- 先预测，再编译或运行；实际与预测不同就记录第一处差异。
- 所有 C++ 代码使用 C++17，并启用 `-Wall -Wextra -pedantic`；线程题额外使用 `-pthread`。
- 故意失败的程序要写清失败阶段、关键诊断、根因和验证证据。
- 每道题的答案直接写在题目下方。
- 四类任务全部达到验收标准，本单元才算完成。

---

## A｜基础概念检查

1. optional 为什么不能代替所有错误类型？

   > 

2. 异常传播时哪些对象会析构？

   > 

3. 什么情况下断言不应替代运行时错误处理？

   > 

4. 用本课最小示例说明：哪个对象、文件或调用在什么语句前后发生了变化？

   > 

---

## B｜可运行编程任务

### 任务

实现 `parse_force` 返回 `optional<double>`：必须用完整字符串、值在 `[0, 1000]`；调用者分别处理有值和无值。

### 起始代码

```cpp
#include <iostream>
#include <optional>
#include <string>

std::optional<double> parse_force(const std::string& text) {
    // TODO: stod + used 位置 + 范围检查 + 异常处理
}

int main() {
    for (const std::string text : {"12.5", "12x", "-1", "1e9999"}) {
        // TODO: 输出值或 invalid
    }
}
```

### 预期结果

```text
12.5 -> 12.5
12x -> invalid
-1 -> invalid
1e9999 -> invalid
```

### 验收标准

- [ ] 没有使用与合法值域冲突的哨兵值
- [ ] 检查 `stod` 是否用完整个字符串
- [ ] 只捕获能够解释和转换的异常类型

### 你的实现

```cpp

```

使用的构建命令：

```bash

```

运行前的输出预测：

> 

实际输出：

```text

```

预测与实际的第一处差异：

> 

---

## C｜调试任务

### 故障代码

```cpp
double parse_force(const std::string& text) {
    try {
        return std::stod(text);
    } catch (...) {
        return -1.0;
    }
}

// 调用者把 -1.0 直接加入平均值，失败数据混入真实数据。
```

### 调试要求

先不要修改。判断问题首先出现在编译、链接、运行、结果还是接口设计；运行或构建后，只保留最关键的诊断。修复时说明为什么最小修改能针对根因，而不是只让报错消失。

运行或构建前的预测：

> 

问题首先出现的阶段：

> 

关键诊断或异常现象：

```text

```

根本原因：

> 

修复后的代码：

```cpp

```

为什么这个修复有效：

> 

修复后的验证输入与结果：

```text

```

---

## D｜求职面试口述

先用 30～60 秒回答本课核心问题。回答必须包含：

- 能让失败在类型或返回协议中可见
- 能解释异常与 RAII 的关系
- 不会用空 catch 隐藏问题

参考结构是“具体问题 → 对象或文件怎样变化 → 使用的规则 → 一个限制”，不要堆术语。

第一次口述提纲：

> 

面试官追问：

> optional 为什么不能代替所有错误类型？

你的回答：

> 

再追问：

> 异常传播时哪些对象会析构？

你的回答：

> 

删掉所有不能回到代码、输出、诊断或测试证据的句子，再写第二版提纲：

> 

---

## 学习复盘

今天最清楚的机制：

> 

仍然不能通过代码解释的地方：

> 

我能否在不看讲义时复现程序和故障：

> 

本单元是否达到四类任务的验收标准：

> 
