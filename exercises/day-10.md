# 第 10 天：地址、指针与引用｜求职版练习

## 作答规则

- 先预测，再编译或运行；实际结果与预测不同时，必须记录差异。
- 所有代码使用 C++17，并开启 `-Wall -Wextra -pedantic`。
- 故意失败的程序必须写明失败阶段、关键诊断和修复依据。
- 每道题的答案直接写在题目下方。
- 四类必做任务全部通过后，本单元才算完成。

---

## A｜基础概念检查

1. 指针对象、指针值和被指向对象有什么区别？

   > 

2. 引用赋值为什么不是重新绑定？

   > 

3. 结合本课最小代码，按顺序说明其中至少三个对象、值或文件发生了什么变化。

   > 

---

## B｜可运行编程任务

### 任务

实现 `bool read_sensor(const double* sensor, double& output)`：允许传入空指针；成功时写入输出对象。

### 起始代码

```cpp
#include <iostream>

bool read_sensor(const double* sensor, double& output) {
    // 检查并读取
}

int main() {
    double value{12.5};
    double output{};
    // 分别测试 &value 和 nullptr
}
```

### 预期结果

```text
有效指针输出 `12.5` 并返回成功；空指针不解引用并返回失败
```

### 验收标准

- [ ] 解引用前检查空值
- [ ] 通过 `const double*` 表达只读目标
- [ ] 通过引用输出参数修改调用者

### 你的实现

```cpp

```

使用的构建命令：

```bash

```

实际输出：

```text

```

预测与实际的差异：

> 

---

## C｜调试任务

### 故障代码

```cpp
int main() {
    int* pointer{nullptr};
    *pointer = 42;
}
```

### 调试要求

使用 ASan 或调试器观察空指针解引用。说明“指针对象存在”为什么不表示“它指向有效对象”。

运行或构建前的预测：

> 

错误发生的阶段：

> 

关键诊断或异常现象：

```text

```

根本原因：

> 

修复后的代码：

```cpp

```

为什么这个修复有效：

> 

---

## D｜求职面试口述

先用 30～60 秒回答：

> 指针对象、指针值和被指向对象有什么区别？

答案必须能够回到本课实验中的具体事实，例如：

- 对象 `offset` 有值和地址，`&offset` 可取得地址
- 引用形参必须绑定对象，适合“必须存在”的输入或输出
- 指针对象保存地址值，可以为空并可重新指向
- 解引用 `*sensor` 前必须证明指针指向仍处于生命周期内的对象

第一次口述提纲：

> 

面试追问：

> 引用赋值为什么不是重新绑定？

你的回答：

> 

删掉任何只能背诵、不能由代码或诊断推导出的句子，再写第二版提纲：

> 

---

## 学习复盘

今天最清楚的机制：

> 

仍然不能通过代码解释的地方：

> 

本单元是否达到四类任务的验收标准：

> 
