# 课程系统重做报告

日期：2026-08-15  
范围：36 份讲义、36 份练习、课程计划、进度、知识补全表和新增示例

## 结论

用户反馈成立。旧讲义的主要问题不是“知识不够完整”，而是把完整性理解成了术语数量和篇幅：读者在看到真实对象或文件之前，就遇到大量专业名词；即使每句话单独看没有明显错误，连起来仍不能回答“它到底拿什么做了什么”。

本次不是只修第 4 天，而是把同一问题按统一标准处理到全部 36 个单元。

## 第 4 天问题到底在哪里

旧句子：

> 狭义编译器是分析 C++ 语法、类型和语义并生成较低层表示或目标代码的工具。

对基础薄弱者无效，原因有四个：

1. “语法、类型、语义、较低层表示、目标代码”在一句话里同时出现，却没有逐个落到代码；
2. 没说输入究竟是原始 `.cpp` 还是预处理后的文件；
3. 没说“较低”是相对谁、具体长什么样；
4. 后面只列职责，没有让读者看到一行代码在阶段前后怎样变化。

新版用同一个程序和真实文件说明：

```cpp
int main() {
    int target{10};
    int current{4};
    return (target - current) * 2;
}
```

| 步骤 | 输入 | 具体动作 | 可观察输出 |
|---|---|---|---|
| 预处理 | `.cpp` 与包含的头文件文本 | 展开 include、处理宏和条件编译 | `-E` 得到 `.ii` |
| 狭义编译 | 预处理后的 C++ 文本 | 确定代码结构、名字指向、类型和运算是否合法；内部可用 IR 表达计算关系 | `-S` 得到目标处理器汇编 `.s` |
| 汇编 | `.s` 汇编文本 | 把助记符和操作数编码成机器指令，并记录符号/待修正地址 | `-c` 得到 `.o` |
| 链接 | `.o` 与库 | 匹配跨文件定义、布局并修正地址 | 可执行文件 |

这里的 **IR（中间表示）** 不是让学习者背诵的神秘文件名，而是编译器内部为了分析和优化使用的数据结构；不同编译器可能有多层 IR。第一遍只需知道它表达了“读两个值、相减、乘二、返回”的计算关系。真正能用课程命令直接观察的是 `-S` 生成的汇编文本。目标文件 `.o` 则是下一阶段的二进制产物，不能与 IR 或汇编文本混称。

## 全课程审计发现

重做前仓库已有 23 份讲义、23 份练习和 87 个示例文件。抽查与结构审计发现：

- 除已经单独整改的第 4 天外，其余 22 天缺少可由实验支撑的求职口述环节；
- 多份讲义在正文主线前放置 13～26 个术语，学习者必须先跨过名词墙；
- 长篇“完整知识地图”没有告诉基础薄弱者今天到底只需掌握哪 3～5 个概念；
- 练习格式不统一，常缺起始代码、预期输出、验收标准或面试追问；
- 正常示例、故障示例和面试答案之间没有稳定的证据链；
- 第 24～36 天只有计划，没有完整讲义、练习和可运行示例。

## 采用的解决方案

### 1. 讲义使用两遍学习

第 1～23 天保留准确的深入内容，但把开头统一改为“第一遍必学”：

> 具体问题 → 最小代码 → 状态变化表 → 3～5 个核心概念 → 求职口述 → 可运行任务与故障实验

原有完整规则放到“第二遍专业细节”。基础薄弱者不再需要先读术语索引或完整知识地图才能开始运行代码。

第 24～36 天按同一逻辑新建，覆盖多态、模板、STL、文件、错误处理、CMake、测试、并发和综合项目。

### 2. 术语只给已经看到的现象命名

第一遍术语表固定包含三列：

| 核心概念 | 人话解释 | 代码/命令证据 |
|---|---|---|

完整术语卡放到文末。暂缓内容必须写明在哪一天补齐；超出 36 天求职基础主线的内容标为进阶扩展。

### 3. 每份练习固定四类任务

- A：基础概念检查；
- B：带起始代码、预期结果和验收标准的可运行任务；
- C：预测阶段、记录诊断、解释根因、修复并回归的调试任务；
- D：30～60 秒面试口述与追问。

答案区直接放在题目下方并保持空白，没有“额外作答占位”或文末汇总区。

### 4. 求职产出贯穿路线

每个单元都要求留下至少一种证据：运行输出、目标文件、Sanitizer 报告、调试记录、测试结果、CMake 项目或面试提纲。综合单元使用机器人关节、设备管理和遥测数据处理，而不是孤立语法题。

## 修改后的覆盖范围

| 项目 | 数量 |
|---|---:|
| 完整讲义 | 36 |
| 固定格式练习 | 36 |
| 新增单元 | 13 |
| 新增第 24～36 天示例源文件/构建文件 | 16 |
| 课程阶段 | 5 |
| 个人学习状态被自动改为完成 | 0 |

## 验证

- 第 24～36 天的 13 个主示例均使用 C++17、`-Wall -Wextra -pedantic` 构建；线程示例额外使用 `-pthread`。
- 13 个示例的实际输出均与讲义预期一致。
- 第 33 天库和应用源文件已用等价的分离编译/链接命令验证，输出为 `clamped = 1`。
- 当前验证环境没有安装 `cmake` 可执行程序，因此没有在本地执行 CMake 配置命令；`CMakeLists.txt` 使用标准 target 写法，并把从干净构建目录运行 CMake 列为验收项。
- 所有最终 Markdown 在提交前检查代码围栏、相对文件链接、同文档标题链接、禁用占位语和 36 天文件完整性。

## 没有替学习者做的事

这次修改只更新教材。第 1 天仍保持“已完成”，第 2、3 天仍保持“待订正”，其余个人状态保持“未开始”。教材齐全不代表知识已经掌握。
