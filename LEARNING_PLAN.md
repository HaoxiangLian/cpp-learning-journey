# C++ 28 个学习单元：基础重建与秋招突击

## 课程定位

- 语言标准：C++17
- 适合基础：能大致判断算法逻辑，但语法、术语和底层模型不扎实
- 单元时长：约 90～120 分钟
- 学习节奏：教材每日发布一单元；个人可以按掌握程度暂停、复习或补做练习

## 教学规则

### 1. 第一次出现的术语必须解释

“对象、作用域、栈帧、链接、构造函数”等词第一次出现时，按以下顺序学习：

> 通俗理解 → 准确定义 → 最小代码 → 执行或内存过程 → 常见错误

不会使用一批尚未学过的名词解释另一个新名词。

### 2. 核心知识使用四步剖析

1. 实际问题：它解决了什么问题？
2. 设计思路：为什么语言要这样设计？
3. 底层机制：程序实际如何工作？
4. 错误做法：写错会发生什么？

底层机制只会在必要前置知识已经掌握后展开。

### 3. 从“会看代码”过渡到“能写代码”

每个单元遵循：

> 读懂示例 → 预测结果 → 亲自运行 → 修改代码 → 脱离示例重写

学习练习先独立思考，再让 AI 检查。工程开发仍可使用 vibe coding，但需要能解释生成代码中的对象、数据流和生命周期。

### 4. 每个单元的固定组成

- 今日目标
- 名词卡
- 核心知识
- 最小示例
- 编译或执行跟踪
- 高频错误诊断
- 动手练习
- 深度思考题
- 完成标准

---

## 第一阶段：真正看懂一段 C++ 程序

目标：建立最基本的语言词汇，能够独立编写小程序。

| 单元 | 学习主题 | 学完必须做到 |
|---|---|---|
| 第 1 天 | 程序、源码、编译与运行 | 看懂最小 C++ 程序，分清源代码、编译器和可执行文件 |
| 第 2 天 | 变量、类型、初始化与表达式 | 理解变量是什么，掌握常用类型、运算符、输入输出 |
| 第 3 天 | `if`、`switch`、循环与代码块 | 能独立写条件判断和循环，初步理解作用域 |
| 第 4 天 | 函数基础 | 分清函数声明、定义、调用、参数、返回值和局部变量 |
| 第 5 天 | 数组、`string` 与 `vector` | 能保存和遍历一组数据，理解下标及越界 |
| 第 6 天 | `struct`、`class` 与对象 | 理解类、对象、成员变量和成员函数分别是什么 |
| 第 7 天 | 第一阶段综合训练 | 独立完成机器人关节状态统计小程序 |

## 第二阶段：建立内存与程序组织模型

目标：逐步理解栈、堆、指针、生命周期、`static` 和链接。

| 单元 | 学习主题 | 学完必须做到 |
|---|---|---|
| 第 8 天 | 地址、指针与引用入门 | 理解 `&`、`*`、`nullptr`，分清对象、地址、指针和引用 |
| 第 9 天 | 栈、堆与对象生命周期 | 理解局部对象为什么会消失，动态对象为何不会自动释放 |
| 第 10 天 | `new`、`delete` 与内存错误 | 识别内存泄漏、野指针、悬空指针、重复释放和越界 |
| 第 11 天 | `const`、`constexpr` 与传参方式 | 分清只读对象、常量表达式、值传递和引用传递 |
| 第 12 天 | `.h`、`.cpp` 与编译链接 | 理解预处理、编译、链接、翻译单元、声明和定义 |
| 第 13 天 | `static`、`extern`、`inline` 与链接属性 | 掌握局部静态对象、全局静态对象、静态成员及内外部链接 |
| 第 14 天 | 未定义行为与调试工具 | 使用警告、调试器和 Sanitizer 定位典型内存错误 |

第 13 天才系统学习 `static`。届时对象、函数、作用域、存储期、翻译单元和链接都已有前置基础。

## 第三阶段：类、对象生命周期与资源管理

目标：理解 C++ 对象模型和资源管理的核心思想。

| 单元 | 学习主题 | 学完必须做到 |
|---|---|---|
| 第 15 天 | 封装、访问权限与构造函数 | 能设计简单类，理解对象如何完成初始化 |
| 第 16 天 | 析构函数与 RAII | 理解对象销毁时如何自动释放资源 |
| 第 17 天 | 拷贝构造与拷贝赋值 | 分清浅拷贝、深拷贝，理解 Rule of Three |
| 第 18 天 | 右值、移动语义与 `std::move` | 理解为什么移动能够减少昂贵拷贝 |
| 第 19 天 | 智能指针与所有权 | 掌握 `unique_ptr`、`shared_ptr`、`weak_ptr` 的基本选择 |
| 第 20 天 | 继承、虚函数与多态 | 理解 `virtual`、`override`、对象切片和动态绑定 |
| 第 21 天 | 第三阶段综合训练 | 编写带资源管理和多态接口的机器人设备模型 |

## 第四阶段：STL、现代 C++ 与秋招工程能力

目标：能够阅读和编写常见工程代码，并形成面试回答体系。

| 单元 | 学习主题 | 学完必须做到 |
|---|---|---|
| 第 22 天 | 函数重载、模板与类型推导 | 看懂基本函数模板、类模板以及 `auto` |
| 第 23 天 | STL 容器及其选择 | 分清 `vector`、`list`、`map`、`unordered_map` 等容器 |
| 第 24 天 | 迭代器、算法与 Lambda | 使用标准算法完成查找、排序、筛选和统计 |
| 第 25 天 | 错误处理机制 | 理解返回值、断言、异常和 `std::optional` 的适用场景 |
| 第 26 天 | 工程结构、CMake 与测试 | 能组织多文件项目，完成编译、链接和基本测试 |
| 第 27 天 | 并发编程入门 | 理解线程、互斥锁、原子变量和数据竞争 |
| 第 28 天 | 综合项目与模拟面试 | 完成代码审查、核心知识复盘和秋招模拟问答 |

## 课程结束时的能力检查

完成 28 个单元后，应当能够：

- 不依赖 AI 独立写出中小型 C++ 控制台程序
- 读懂常见类、容器、指针与资源管理代码
- 解释一段代码中对象的创建、使用和销毁过程
- 定位常见编译错误、链接错误和运行时内存问题
- 为机器人相关的小型项目组织多文件结构、CMake 和基本测试
- 用自己的语言回答常见 C++ 秋招问题
