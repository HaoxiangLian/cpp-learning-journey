# 知识完整性与补全追踪表

更新日期：2026-08-15  
当前审计范围：第 1～36 天求职版讲义、示例和练习

## 用途

这份表防止两种相反问题：

1. 为了“简单”而隐藏真实模型，最后形成错误理解；
2. 为了“完整”而在第一遍堆入大量当前用不到的细节。

课程现在把内容分为：

- **第一遍必学**：能运行、能跟踪对象/文件变化、能调试、能面试口述；
- **第二遍专业细节**：补语言规则、工具边界和易混点；
- **进阶扩展**：超出 36 单元求职基础主线，明确保留名称，但不冒充已经深入覆盖。

“教材已发布”和“学习者已掌握”仍是两件事。本次重做没有修改个人掌握状态。

## 追踪表

| 编号 | 早期容易形成的空洞或错误模型 | 必须建立的准确主线 | 补全单元 | 教材状态 | 学习状态 |
|---|---|---|---|---|---|
| G01 | 把 `.cpp → 编译 → 可执行文件` 当完整过程 | 驱动、预处理、狭义编译、汇编、目标文件、链接、装载 | 4、15、16、33 | 求职主线已发布 | 未通过对应练习验证 |
| G02 | 把 `g++` 当成内部只有一步的工具 | 区分驱动程序和各阶段工具 | 4、16 | 已发布 | 未通过对应练习验证 |
| G03 | 把 `#include` 只说成“获得功能” | 文本包含、保护、自包含头、翻译单元与 ODR | 4、15、17 | 已发布 | 未通过对应练习验证 |
| G04 | 把 `main` 说成操作系统直接进入的原始地址 | 运行库启动、进入 main、退出状态 | 4、5 | 已发布 | 未通过对应练习验证 |
| G05 | 把 cout/cin 当普通输出输入函数 | 流对象、运算符重载、链式表达式、缓冲和状态 | 25、31 | 已发布 | 未通过对应练习验证 |
| G06 | 把变量只说成数据盒子 | 对象、名称、类型、存储、值、存储期和生命周期 | 2、8、11、13、19 | 已发布 | 未通过对应练习验证 |
| G07 | 基本类型只按整数/小数分类 | 范围、精度、表示、符号性和转换 | 2、13、14 | 已发布 | 未通过对应练习验证 |
| G08 | 只教一种初始化写法 | 列表/直接/复制/值/默认初始化与窄化 | 2、13、19 | 已发布 | 未通过对应练习验证 |
| G09 | 把 string 当神秘文本类型 | 类、资源管理、模板、复制/移动和容器关系 | 7、19～23、27、28 | 已发布 | 未通过对应练习验证 |
| G10 | 只展示 `cin >> value` 成功路径 | 提取失败、状态恢复、残留字符、getline 与解析 | 31、32 | 已发布 | 未通过对应练习验证 |
| G11 | 把名字离开作用域等同于对象立即消失 | 作用域、存储期、生命周期、链接四个维度 | 3、11、17 | 已发布 | 未通过对应练习验证 |
| G12 | 只背 `&&` / `\|\|` 真值表 | 短路、求值顺序与副作用 | 3、14 | 已发布 | 未通过对应练习验证 |
| G13 | 把 `++x` 永远等同于 `x=x+1` | 前/后置结果、副作用及重载边界 | 14、25 | 求职主线已发布；自定义自增重载为扩展 | 未通过对应练习验证 |
| G14 | 只说未初始化值“危险” | 不确定值读取、UB 和工具证据 | 11、14、18 | 已发布 | 未通过对应练习验证 |
| G15 | 用“栈/堆”代替语言生命周期规则 | 存储期规则与常见实现内存区域分开 | 11、12 | 已发布 | 未通过对应练习验证 |
| G16 | 值/引用传参只停在基本类型 | 参数对象、绑定、const、复制/移动和值类别 | 6、10、13、14、21、22、27 | 求职主线已发布；转发引用/引用折叠为进阶 | 未通过对应练习验证 |
| G17 | 重载只按参数个数背诵 | 名称查找、转换序列、成员/非成员和模板重载 | 6、8、13、17、25、27 | 求职主线已发布；复杂 ADL/用户转换为进阶 | 未通过对应练习验证 |
| G18 | 递归只背基例和递归式 | 调用对象生命周期、栈资源、诊断与尾调用边界 | 6、11、18 | 已发布 | 未通过对应练习验证 |
| G19 | 把数组当不会退化的固定容器 | 数组类型、指针转换、形参调整、越界与 std::array | 7、10、14、28 | 已发布 | 未通过对应练习验证 |
| G20 | vector 只会 push_back | 大小/容量、复杂度、复制移动、重分配和失效 | 7、21、22、27、28 | 求职主线已发布；allocator 为进阶 | 未通过对应练习验证 |
| G21 | 只比较 `[]` 和 at 的语法 | 越界 UB、容器保证、Sanitizer 与错误策略 | 7、14、18、28、32 | 已发布 | 未通过对应练习验证 |
| G22 | 范围 for 只当简写 | begin/end、迭代器、值/引用循环变量和算法 | 7、14、28～30 | 已发布 | 未通过对应练习验证 |
| G23 | 枚举只当带名字整数 | 作用域、底层类型、转换、表示与 ODR | 8、13、14、17 | 已发布 | 未通过对应练习验证 |
| G24 | 把 using 当纯文本替换 | 别名对接口/查找的影响及与强类型的区别 | 8、17、27 | 求职主线已发布；别名模板为进阶 | 未通过对应练习验证 |
| G25 | 命名空间只当文件夹 | 名称查找、链接、ODR 与 ADL 位置 | 8、15～17、25、27 | 求职主线已发布；内联命名空间为进阶 | 未通过对应练习验证 |
| G26 | 类只讲成员和访问权限 | 构造析构、this、复制移动、继承与多态 | 8、11、19～24 | 已发布 | 未通过对应练习验证 |
| G27 | 指针只讲地址和解引用 | cv、转换、生命周期、所有权与智能指针边界 | 10～14、17、23、30 | 求职主线已发布；成员指针等为进阶 | 未通过对应练习验证 |
| G28 | 用裸 new/delete 作为工程默认 | RAII、Rule of Zero、智能指针和容器 | 12、20～23、28 | 已发布 | 未通过对应练习验证 |
| G29 | constexpr 只当 const 的加强版 | 常量表达式、函数、模板/类型推导与构建位置 | 13、14、27、33 | 求职主线已发布；字面类型/if constexpr 深入为进阶 | 未通过对应练习验证 |
| G30 | 只从单线程看求值先后 | 运算符调用、线程同步、数据竞争与先发生关系 | 14、25、35 | 求职主线已发布；内存序为进阶 | 未通过对应练习验证 |
| G31 | 头文件只讲保护宏 | 模板定义可见性、目标 include 目录和构建定义 | 15、17、27、33 | C++17 求职主线已发布；C++20 模块不在本计划 | 未通过对应练习验证 |
| G32 | ELF 实验被误当跨平台全部规则 | 符号/重定位/库与 CMake 目标；区分 ABI 扩展 | 16、17、25、27、33 | 求职主线已发布；SONAME/RPATH/ABI 深入为进阶 | 未通过对应练习验证 |
| G33 | 静态对象只按“一直存在”理解 | 存储期、初始化/销毁依赖和函数局部静态替代 | 17、19、20、33 | 求职主线已发布 | 未通过对应练习验证 |
| G34 | 调试只覆盖单线程正常进程 | GDB、ASan/UBSan/LSan/TSan、测试与可复现配置 | 18、33～35 | 求职主线已发布；远程调试/core dump 为进阶 | 未通过对应练习验证 |

## 发布门槛：六问检查

任何过程类讲解发布前都必须能回答：

| 问题 | 必须提供的证据 |
|---|---|
| 它为什么存在？ | 没有它会出现的具体问题 |
| 它拿到什么？ | 代码、对象、文件、数据或状态 |
| 它具体改变什么？ | 同一个可观察对象的前后变化 |
| 它交出什么？ | 产物、值、状态或诊断 |
| 怎样验证？ | 命令、输出、调试器、Sanitizer 或测试 |
| 面试怎样问？ | 30～60 秒、能由实验推导的回答 |

“负责分析、优化并生成某种表示”而没有输入、变化、输出和验证方法，不算合格解释。

## 维护规则

1. 新术语第一次参与推导时，先给人话解释，再给准确边界和代码证据。
2. 第一遍每单元只保留 3～5 个核心概念；额外术语进入第二遍或附录。
3. 暂缓内容必须写明后续单元；超出 36 天的内容标为“进阶扩展”。
4. 教材发布只能更新“教材状态”，不能更新学习状态。
5. 只有配套练习经过批改并达到验收标准，才能把相应学习状态改为已掌握。
